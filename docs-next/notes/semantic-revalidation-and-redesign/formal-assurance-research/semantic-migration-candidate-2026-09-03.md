# Semantic Migration Candidate Record

> **Status:** branch-only research candidate; `Hold`
> **Baseline:** `4ff8358108b6acf4355fa178b9b70529c6fb3778`
> **Authority:** none; no owner page, profile identity, or publication table is
> changed by this record

## 1. Question

What concrete successor results when the selected Terminal repair, normalized
owner-view publication, endpoint Terminal obligations, and Foundation/
PublicCoin wording corrections are considered together, while publication,
identity finalization, and the three provider-observable ownership decisions
remain forbidden?

The executable answer is
[`evaluation/semantic-migration-candidate/`](../../../../evaluation/semantic-migration-candidate/README.md).

## 2. Result

Two independent semantic-profile compilers agree on the complete candidate
table. With the Foundation law source held fixed, Interaction and its fifteen
transitive dependents rotate; `oir-endpoint-graph` and `analysis-kernel` remain
stable. The candidate Interaction digest is
`62464076d1ed690fa1acf93998cae4cdc3600649b5af028d4390d3046a3bb7a7`.
It is not a published or selected identity.

The overlay records exact before/after bytes and unified diffs for six owner
pages and seven profile manifests. It adds one finite endpoint Terminal fixture
that retains required Check, Reduction, Claim, output, verdict, and spine
coordinates while deriving Claim disposition from verdict. Four old-shape and
omission controls refuse.

## 3. F1 consequence

The migrated prerequisites do not preserve the old gates byte-for-byte:

- R1A becomes successor-profile agreement plus explicit refusal of the frozen
  old profile row.
- R1B re-encodes Terminal rows and rotates the bounded Schnorr Core and Fresh
  Protocol IDs. The translated finite slice admits and the old Terminal bytes
  differ.
- R1C0's six missing publication premises become affirmative: six schema
  declarations, one closed 88-definition grammar, and six exact source-body
  routes are present. Full provider correspondence remains downstream.

Thus the migration repairs the source prerequisite rather than proving the
later correspondence result.

## 4. Open alternatives

No provider-observable ownership choice is made. The executable record shows
both sides of each question:

1. exact algorithm bytes in an owner view rotate the Interaction cone; exact
   K1 preimages in the formal-source package do not add PIR rotation;
2. a PIR denotational public-coin body rotates the Interaction cone; an
   Analysis distribution binding does not add PIR rotation; and
3. a PIR-owned outcome map rotates the Interaction cone; per-provider Analysis
   maps do not add PIR rotation.

The three PIR-owned alternatives compile to three distinct Interaction
digests, confirming that they are semantic choices rather than documentation
wording.

## 5. Foundation and active-lane slots

The natural-byte-bound correction remains parameterized until the active
Foundation lane reports. Evaluator alignment to the current law keeps the
16-profile cone; changing the law source rotates the Foundation regime and all
18 profiles.

Explicit slots remain for the Fresh run schema, family-local Fiat--Shamir view
determinacy, integrated provider audit, Foundation result, and current
gate-reclosure result. Their absence is a `Hold`, not a guessed conclusion.

## 6. Decision boundary

The package is ready to inform a later owner decision, but not to be promoted
wholesale. Publication requires the open owner decisions, active-lane inputs,
one refreshed holdout pass, and independent freeze review. The current target
and checked-in identity table remain unchanged.
