# PIR migrated-owner refreeze rehearsal

> **Kind:** non-authoritative research handoff
> **State:** semantically rebased rehearsal complete; identity publication
> intentionally withheld
> **Starting branch:** `docs/pir-migration-v2c`
> **Starting commit:** `8346f68b03194736fae94b09f3c4cced96f6d7a3`
> **Research branch:** `lane/v2c-refreeze-rehearsal-2`

## Outcome

The migrated PIR owner text can be consumed directly by the dependent research
packages at the recorded starting commit. Positive paths read the current
owner pages and profile manifests; they no longer depend on synthetic profile
overlays. Copies of current sources remain only where a negative mutation needs
an isolated input.

Both semantic-profile publication compilers reconstruct the same eighteen-row
candidate table. Relative to the checked-in pre-migration publication table,
seventeen profiles rotate and `analysis-kernel` alone remains stable. Relative
to the first refreeze rehearsal, sixteen profiles rotate; `analysis-kernel` and
`oir-endpoint-graph` remain stable. The checked-in publication table is not
modified.

The two source defects reported by the first rehearsal are closed in the
current owner text. `PIRReference` has a finite definition, and the Interaction
owner view has an exact five-entry law-field selection. The canonical-framed
and duplex Fiat--Shamir pages additionally select their family-local law
fields, with manifest dependencies and revision changes reflected in the new
candidate cone.

This rehearsal does not authorize or perform identity publication.

## Candidate roots

The candidate table was reconstructed outside the repository tree with:

```sh
python3 -B evaluation/semantic-profile-publication/run.py --print-identities
```

The complete output was retained in scratch space for this lane. Its table
SHA-256 is
`9a59aa4511a8888803a52fadcd9531e3db917b25925140d1507b9cd80c13473e`.

| Coordinate | Candidate value |
|---|---|
| Interaction revision | `2` |
| Interaction profile digest | `2a1d4f1429b25fcd315072b654f6f0a6816e167d3c06a3a0f29b8028a023349f` |
| Interaction profile body SHA-256 | `28fe377ff6cae5799ba243d02b6ccc8b3f84d248c40521960d42b34ec44a1b1f` |
| Finite target Core ID | `zkcidv0:pir.interactive-core:f11d422776ac28d192cc13a1c276049b0dae211394e344ad9e87008f92c4e2f2` |
| Finite target Core body SHA-256 | `d0de3468542e54ff2de5819701bff9567d223f1ecedc749c17b9d842d904d30d` |
| Finite Fresh Protocol ID | `zkcidv0:pir.protocol:4e11f03b4f5d65e5009b703bc5c78b8c186851888f95f7a52774b7e923a319f9` |
| Finite Fresh Protocol body SHA-256 | `e42db068c9288399927465a4c82974e966e8b51cb95b8bd1a198f3eabf19e8ec` |

## Package-by-package result

“Aggregate changed” compares the package disposition in the first rehearsal
with this semantically rebased rehearsal. A changed finding inventory under an
unchanged aggregate is stated separately.

| Package | Rebased rehearsal change | Aggregate changed? | Owner-text defect found |
|---|---|---|---|
| `evaluation/expressibility-axes` | Re-pinned the migrated owner source used by structural classification. | No. | None. |
| `evaluation/family-instance-probe` | Re-pinned family identities and expected source observations. | No. | None. |
| `evaluation/formal-source-target-basis-f1r1a` | Reconstructed revision-two Interaction directly, added an explicit candidate pin, and retained the old publication row as a refusal control. | No. | None. |
| `evaluation/formal-source-target-core-f1r1b` | Reconstructed the finite Core and Fresh Protocol under the current migrated profile and froze their rotated identities. | No. | None. |
| `evaluation/formal-source-owner-views-f1r1c` | Checks the current schema catalog, bodies, routes, and exact five-entry law-field selection directly. All thirteen observations are affirmative. | **Yes:** `CannotAnswer` to `Affirmative`. | None; the former law-selection defect is closed. |
| `evaluation/formal-source-owner-view-repair-f0v` | Reads current manifests directly and measures the seventeen-profile cone against a pinned pre-migration control table. | No; remains affirmative. | None. |
| `evaluation/formal-source-view-body-audit-f0v2b0` | Re-transcribed the migrated static-view grammar. `PIRReference` and all owner-body source obligations are now affirmative except bounded constructor coverage. | No; remains `CannotAnswer` on bounded coverage. | None; the former `PIRReference` defect is closed. |
| `evaluation/formal-source-view-bodies-f0v2b1` | Re-pinned the normalized finite derivation to the migrated profile. | No. | None. |
| `evaluation/formal-source-constructor-closure-f0v2b2a` | Updated the census to fifteen canonical tables with 55 cases and six derived algebras with 24 cases. | No. | None. |
| `evaluation/formal-source-view-schema-f0v2b2b` | Re-pinned the 88-declaration schema and predecessor digest to the migrated profile. | No. | None. |
| `evaluation/formal-source-owner-projections-f0v2b2c1b1` | Re-pinned independent profile reconstruction used by the finite owner projections. | No. | None. |
| `evaluation/formal-source-terminal-owner-contracts-f0v2b2c1b5b1` | Converts the former gap demonstration into a regression over the migrated Terminal contract: the repaired carrier admits and the hidden-gating counterexample refuses. | No; remains affirmative. | None. |
| `evaluation/formal-source-terminal-owner-projections-f0v2b2c1b5b2` | Reads current manifests directly, retains only the finite Core as a synthetic pressure fixture, and refreshes predecessor pins. | No; remains affirmative. | None. |
| `evaluation/formal-source-integrated-graph-f0v2b2d1` | Reads current manifests directly and checks the migrated transfer, precedence, LogicalAccess, Query, and acceptance-sink clauses affirmatively. | No; remains affirmative. | None. |
| `evaluation/formal-source-fresh-run-schema-f0v2b2d2` | Reads current manifests directly; fixation, receipt prefix, partial record, strategy-stop, and outcome rules are affirmative source checks. | No; remains affirmative. | None. |
| `evaluation/formal-source-integrated-views-f0v2b2d3` | Re-freezes the 30 projected bodies and adapted provider ledger after predecessor re-pinning. The 8,402-leaf universe and 40-to-zero structural-gap result are unchanged. | No; remains affirmative. | None. |
| `evaluation/formal-source-fs-view-determinacy-f0v3` | Reads the two current family pages and manifests directly, audits 95 fields, verifies the exact family law selections, compiles eight schemas, checks eleven available values, and measures the full cone. | No; remains affirmative. | None; the proposal obligations are absorbed. |
| `evaluation/formal-provider-observables-f2o0` | Re-emitted the finite provider artifact and ledger under migrated target identities and refreshed its elaboration receipt. | No. | None. |
| `evaluation/formal-provider-observables-f2o1` | Re-pinned the integrated Core and Fresh Protocol and regenerated the digest-bound ledger and elaboration receipt. | No; remains `CannotAnswer` on provider observables. | None. |
| `evaluation/formal-schnorr-relations-plan-f2p0` | Re-pinned the relation/Plan contract ledger to the migrated finite subject. | No; remains `CannotAnswer`. | None; its claim-free fixture is a declared package limit. |
| `evaluation/formal-schnorr-relations-plan-f2p1` | Re-pinned current Interface/Plan source, the rotated finite Protocol, and the upstream relation/Plan result while preserving all 39 finding meanings. | No; remains `CannotAnswer` because the finite fixture has no initial Claim reference. | None; the missing initial Claim is a fixture boundary. |
| `evaluation/semantic-migration-candidate` | Repoints the package to six current pages, eight current manifests, both publication compilers, legacy refusals, and downstream regressions. Exact owner-view law binding now passes. | No; remains `Hold` solely for publication. | None. |

The current mechanization package was not merged from the first rehearsal. Its
next increment is owned by another lane. The provider-interpretation package
was also left untouched because its next round belongs to the Analysis branch.

## Rebase

### Recorded base

- Current source branch at lane creation: `docs/pir-migration-v2c`.
- Exact starting commit:
  `8346f68b03194736fae94b09f3c4cced96f6d7a3`.
- Research branch: `lane/v2c-refreeze-rehearsal-2`.
- First rehearsal source: commit `5f4250d` on
  `lane/v2c-refreeze-rehearsal`, based on
  `975d1e98a61880b800f92efe9c115dd728260113`.

Each package patch was treated as a semantic source, not applied as a textual
commit replay. Current owner text and manifests were the target. Catalog,
lifecycle, research-index, and structural-axis overlaps were merged at entry
granularity. No prior mechanization changes were taken.

### Identity delta from the first rehearsal

The first rehearsal table SHA-256 was
`95358c668eb614baaaac6c2041ccce6ab8f2e7077ada0bb53b8791ddf4d1e8e2`.
Sixteen of eighteen profile digests differ from that table. The two stable
rows are `analysis-kernel` and `oir-endpoint-graph`. Interaction and both
Fiat--Shamir profiles advance from revision one to revision two.

| Profile | First rehearsal digest | Current candidate digest | Delta | Revision |
|---|---|---|---|---|
| `interaction` | `b5fc323b8f87b28d32db732234de57e3bf3f912052294ecb648e985d1d639902` | `2a1d4f1429b25fcd315072b654f6f0a6816e167d3c06a3a0f29b8028a023349f` | Rotated | 1 → 2 |
| `canonical-framed-fiat-shamir` | `bb67eeb9cd90579e9a4d4a7dc5ceb6b555a67d769523ef366feb9792552226bf` | `fe0cfa79211bf2d8290b749b191ce311306df3321231ed09f9c1ae26269ea43c` | Rotated | 1 → 2 |
| `duplex-sponge-fiat-shamir` | `cd7d32afa4ec864385befa978ca93082b0dde7f60fedd6e494b2000edf1ef8a7` | `257118b591f2040f4a1e1a243b9069825b97159b521a6640c507ded47ea79736` | Rotated | 1 → 2 |
| `public-setup` | `6c8c48defee334573b7d401733001922fa2fc294a213d10a8bbeffc46d099f20` | `e913ccd3f093237f209a75ae3a8feb46822a9129df1a85a045f977957b56b123` | Rotated | 1 |
| `commitment-opening` | `ecd280122594940d3859b7f1bdfb50672d00c1a1d47df3600feb85a82475bd3a` | `12b4c64055b494037f928ae961c41f3f8fe035078d01b173b78d4fcdd8c845c6` | Rotated | 0 |
| `oracle-commitment` | `54b5145e8d5ffcdb57c6e45bfb9117d3b79aaeaf817b0de46b24cf3f90bf74c7` | `2318fde703e4e106cef175b69e9f2d484b62cb15eb5ce60f01a3f89def857db6` | Rotated | 0 |
| `verifier-derived-query-plan` | `1dc00e6b5d414ee035d695ce6df508730d3ecdce085037d24df0dda5e42bd244` | `25a5b8875e04d35fa7e68eea644fb18eeed6a095cf4c025efe74c626b5f5552c` | Rotated | 0 |
| `interface-plan` | `43886c2152e1dff6542f4f3ebcdd3e79f6584594b3bbb6698816a6fe17da4a6f` | `49b8ff1008ed87fa4e46a7120bbb7afedccf76e0f4fa1757478b537c6f58b1a1` | Rotated | 1 |
| `oir-endpoint-graph` | `6ae3d69a557442ca34b889326c09513bee9545ba78c6a49e0c0d99a9c823b4b7` | `6ae3d69a557442ca34b889326c09513bee9545ba78c6a49e0c0d99a9c823b4b7` | Stable | 1 |
| `endpoint-source-view` | `2493f7f40945bbea635c09b72ad0a442e58ba4720b58706c6cd97b8e5d49ee06` | `19b9711f40a8e31a47d2216bae936c0a8f95739215e80894c8406897c638031b` | Rotated | 1 |
| `oir-projection-relation` | `c7905643e6514871d04a4412ea229c462573f478677e3b5bd369688c0b88b75c` | `1f02f55301705c0e4c78b2399460c329364d82691408cb47c083b9352a68fd2c` | Rotated | 1 |
| `relations` | `054ed87bfc70587097b429ec59048a78732f305d18e2777ec9db41d0e89e1e8f` | `41f8c2cee3076fd95bfde1a58fd243db3b640e3f73454f97dfd7281287d90cb8` | Rotated | 0 |
| `analysis-kernel` | `aa5c64e33adf55fa3b7ae770a05e7f32d60240c12f0010394a469f6fa1076166` | `aa5c64e33adf55fa3b7ae770a05e7f32d60240c12f0010394a469f6fa1076166` | Stable | 0 |
| `analysis-cryptographic-property` | `c2ad88fd40113bdf84d2d0ef083b162fba15c7e467d223f880cc669a781ca68c` | `cf6755e18efba8ff90caca2dc875bc51a0f4ddcca55994d30e652655010fab49` | Rotated | 0 |
| `analysis-afk-transport` | `1daf17539ba077f2b28f86cee89edb839a3cb7a62a27b75c10e18d39f747bd47` | `337f354219a12c6b4d350bd276e682e2d2eaa4dcd21e18256b72870a7ee9dd3f` | Rotated | 0 |
| `analysis-afk-theorem-source-validation` | `b845cbdf866ce969d2dd73ffe5052505486e018bc2dcbd6251e9465e6a22bbdd` | `45f947fae288f672a0abeb0ccb5d816820629522334b54b9674e920c4a27bd14` | Rotated | 0 |
| `analysis-incremental-composition` | `5383d2e00886acbd324d6be08e09b217e9ad3665ce9b9eb28616fab3c98a8ed2` | `a77ea73c92b62561dafbe87db572c3c0334c4d59c4d50fea4854a8c9ec751081` | Rotated | 0 |
| `analysis-incremental-composition-source-validation` | `4189a29f3037a8971299c448f951bc869d9fc5c035a2e2defd9d8de451a1d9cd` | `5e056064ef920b4fa9cf163f67a7c3f8697a29c46730971b31bdf2203b1cc9f2` | Rotated | 0 |

### What a real refreeze must repeat

1. Obtain explicit authority to publish the selected identities.
2. Reconstruct the identity table from the exact committed owner pages and
   manifests with both publication compilers; do not reuse this scratch table.
3. Write the publication table through the official publication path, then
   replace every rehearsal candidate pin with the published row and regenerate
   all digest-bound fixtures and receipts.
4. Integrate and rerun the independently owned migration-text,
   holdout-readjudication, Analysis-premise, provider-interpretation, and
   mechanization results at their landed commits.
5. Rerun focused changed-package checks and the complete publication and
   release checkpoints with the ordinary repository index on a clean tree.
6. Commit the refreeze only after publication, old-profile refusal controls,
   downstream source correspondence, and lifecycle/index updates agree.

## Excluded ownership

The following packages were left byte-for-byte untouched:

- `evaluation/formal-source-migration-text-review-f0v2c1/`;
- `evaluation/formal-source-holdout-readjudication-f0v2c2/`;
- `evaluation/formal-source-analysis-premise-review-f0v2d1/`;
- `evaluation/formal-provider-interpretation-f2o2/`; and
- `evaluation/formal-kernel-mechanization-m0/`.

No owner page under `docs-next/pir/`, `docs-next/foundation/`,
`docs-next/relations/`, `docs-next/analysis/`, or `docs-next/oir/` was edited.
`docs-next/pir/profiles/published-identities.json` was not edited.

## Focused validation

The 21 carried package checks pass together under the offline environment.
This covers the structural axes, family instance, target basis and carrier,
owner-source audits, constructor and projection regressions, integrated graph
and six-view packages, Fresh and Fiat--Shamir packages, both provider-observable
audits, both Schnorr relation/Plan packages, and the non-publishing migration
regression. This bounded result is not a publication or semantic proof.

## Final checkpoint

The requested full checkpoint was run once under offline dependency settings
and the clone-local alternate index:

```sh
python3 -B checks/run.py run --tier research-checkpoint --keep-going
```

The immutable controller result is
`target/checks/20260903T112137Z/result.json`. It completed all 62 checks in
776.143 seconds: 57 passed and five failed. Every package carried by this
rehearsal passed. The exact red set and its interpretation are:

- `research.profile-publication`: expected publication hold. The publication
  compiler reconstructs the candidate table, but the checked-in table still
  contains the pre-publication identities.
- `research.migration-text-review`: `CannotAnswer` at this base pending its
  separately owned fifth review. The checker at
  `evaluation/formal-source-migration-text-review-f0v2c1/run.py:1557`
  requires every migrated profile transition to end at revision one; the
  current Interaction, canonical-framed, and duplex manifests are revision
  two after the newly selected law declarations.
- `research.holdout-readjudication`: `CannotAnswer` at this base pending the
  same review lane. Current owner and support bytes no longer match its pins,
  so the package emits `migrated-owner-page-bytes` and
  `migration-and-axis-source-bytes` as `CannotAnswer` and then refuses the
  stale frozen finding projection at
  `evaluation/formal-source-holdout-readjudication-f0v2c2/run.py:660`.
- `research.provider-interpretation`: `CannotAnswer` pending its separately
  owned Analysis-branch round. Its generated certificate still pins the first
  rehearsal Core, Protocol, six-view bodies, and integrated-view finding
  digest; regeneration produces the current rotated subject and bodies, so
  `evaluation/formal-provider-interpretation-f2o2/generator.py:336` refuses
  the stale certificate.
- `research.kernel-mechanization-feasibility`: expected pending the separately
  owned mechanization increment. Its source-pin check at
  `evaluation/formal-kernel-mechanization-m0/run.py:358` refuses the changed
  `evaluation/formal-source-holdout-readjudication-f0v2c2/expected-findings.json`.

The intended two-red state cannot be demonstrated at this exact base without
editing packages that this lane was explicitly forbidden to touch. These
three additional reds are owned-lane synchronization gaps, not refreeze-owned
failures or newly found owner-text defects. The source branch subsequently
received the separately owned review and holdout update as `d4e5177d`; that
commit was not imported into this fixed-base lane. Main should reconcile the
owned updates, refresh the rehearsal after the later owner change described in
the handoff, and then rerun the affected checks narrowly.

## Non-claims

This rehearsal establishes neither published semantic identity nor general
source correspondence, arbitrary-Core projection, runtime or replay
correctness, implementation conformance, theorem applicability, protocol
soundness, Fiat--Shamir security, random-oracle or quantum-random-oracle
security, backend correctness, or deployment readiness. Green packages remain
bounded evidence for their exact sources, fixtures, and mutations.

## Handoff

- Worktree: `/home/wonjae/code/zkc-lanes/v2c-refreeze-rehearsal-2`.
- Branch: `lane/v2c-refreeze-rehearsal-2`.
- Exact base: `8346f68b03194736fae94b09f3c4cced96f6d7a3`.
- At handoff, `docs/pir-migration-v2c` had advanced to
  `7f65c48dc83c490f286ec04add6e3deace6bc6c8`, two commits beyond the fixed
  base. `d4e5177d` carries the separately owned fifth review and holdout
  re-pins. `7f65c48d` generalizes the `PIRReference` declaration-kind arm and
  changes the Interaction owner source. Main must merge the two index/catalog
  overlaps at entry granularity, recompute the candidate table, and re-pin the
  carried packages to the generalized owner text; identities recorded here
  apply only to the exact base above.
- The tree contains only carried package re-pins and transcriptions, generated
  receipts, catalog/lifecycle/index reconciliation, and this research note.
- The actual Git metadata is read-only. Nothing was staged in the ordinary
  index, committed, pushed, or submitted as a pull request. The final check
  uses a clone-local alternate index only.
- Full checkpoint result: 57 of 62 passed. The five-red set and exact ownership
  reasons are recorded above; all 21 carried checks passed both in the focused
  run and inside the full checkpoint.
- Main should inspect the dirty tree, integrate later owned-lane results at
  entry granularity, and commit with subject:
  `test: rebase the refreeze rehearsal onto the closed owner text`.
