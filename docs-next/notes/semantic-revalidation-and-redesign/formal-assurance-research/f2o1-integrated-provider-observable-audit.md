# F2-O1 Integrated Provider-Observable Audit

> **Kind:** Temporary bounded provider-side audit of the D1 integrated carrier
> **State:** `CannotAnswer/F2O1-C-MISSING-OPERATIONAL-OBSERVABLE`; 56
> operational observables and five property premises have no source coordinate
> in the normalized owner-view bodies actually issued for this authority
> **Authority:** None. The package changes no target semantics, owner page,
> profile identity, evaluator, Analysis judgment, or deployment policy.
> **Executable evidence:**
> [`evaluation/formal-provider-observables-f2o1`](../../../../evaluation/formal-provider-observables-f2o1/README.md)

## 1. Question and answer

F2-O1 asks whether the exact D1 `integrated-baseline` Core and Fresh Protocol,
through their six normalized owner views, carry every operational observable
needed for one generic VCVio `OracleComp`/`ProbComp` interaction over all 23
occurrences. This subject adds the discriminators F2-O0's Schnorr slice could
not exercise: independent, joint, and shared Challenges; all three Oracle
modes; Public and Verifier-only queries; all three semantic-module decision
classes; Claims and Reductions; and three first-active terminals.

The answer is `CannotAnswer`, but not because those discriminators are absent
from the authenticated Core. The source package is incomplete at an earlier
boundary. D1 derives one exact `PublicCoinView` and no integrated bodies for
the other five normalized views. B5B2's six bodies belong to its own terminal
fixture and evaluator authority, not D1's carrier. Reusing them here would be a
cross-subject source substitution.

The generated ledger therefore claims only 32 distinct leaves of D1's
906-leaf `PublicCoinView`. Fifty-six operational constructs remain typed gaps;
three Claim predicates and two Reduction obligations remain separate
property-premise gaps. No missing view
or denotation is synthesized from the Core, target prose, or module preimages.

## 2. Exact source boundary

D1's implementation exposes `project_public_coin` at
`evaluation/formal-source-integrated-graph-f0v2b2d1/model.py:2330-2381`.
There is no D1 projection returning integrated `PublicBindingView`,
`StrategyDecisionView`, `EffectView`, `ClaimReductionView`, or `ExecutionView`
bodies. The target's Section 13.2 describes all six bodies at
`docs-next/pir/interactive-core.md:2046-2131`, closes constructor-specific
reads at lines 2156-2170, and says a view adds no fact absent from its exact
admitted owner body at lines 2172-2174. Those statements identify the intended
owner boundary; they do not instantiate missing D1 bodies.

This is recorded as
`CannotAnswer/F2O1-C-INTEGRATED-VIEW-PROJECTIONS`. It is not evidence that the
target bodies are wrong and does not propose an owner-page edit. It says only
that this exact D1 package does not supply five artifacts that the requested
view-only audit presupposes.

## 3. Method and mutation evidence

The untrusted generator renders one abstract operation per Core occurrence.
It samples shared Challenge 0 once and passes the same Lean value to both
Reductions; records joint-group indices and Challenge 2's prior member;
separates Oracle mode from provider carrier representation; and nests Accept,
Abort, and fallback Reject in first-active order. Every construct has one
ledger marker and exactly one claimed view leaf or typed gap.

The checker imports no generator code. It authenticates and byte-decodes the
Core and Fresh Protocol through D1's cold path, authenticates their semantic
module and portable-algorithm closure, independently derives
`PublicCoinView`, and enumerates every active leaf. It checks occurrence
totality, coordinate validity and injectivity, exact gap enumeration, Lean
marker consistency, the integrated discriminator values, and schedule order.

The following directed mutations all refuse with named codes:

- two draws replacing the one shared-Challenge value:
  `F2O1-R-SHARED-CHALLENGE-DUPLICATED`;
- reordered interleaved Query/Answer occurrences:
  `F2O1-R-SCHEDULE-ORDER`;
- a missing Reduction-to-Challenge backlink:
  `F2O1-R-REDUCTION-CHALLENGE-BACKLINK`;
- LogicalAccess rewritten as a committed table:
  `F2O1-R-ORACLE-MODE-MISMATCH`;
- the Verifier-only Query rewritten as Public:
  `F2O1-R-QUERY-VISIBILITY`; and
- omitted fallback-terminal preemption:
  `F2O1-R-TERMINAL-PREEMPTION`.

The generated file elaborates at VCVio revision `de0a3108` under Lean v4.33.1
with an empty axiom closure. The digest-bound receipt is an environment fact,
not semantic correspondence.

## 4. Gap classification

The exact operational list is frozen in the package's
`expected-findings.json`. It contains:

- `operational-owner-view` gaps for the Protocol identity, the 20
  non-Challenge occurrence rows, the three module decision classifications,
  all Oracle modes and query visibilities, Claims, Reduction declarations,
  Terminal declarations, baseline fallback-terminal preemption, and the
  fallback-Accept preemption coordinate in the logical-reject control;
- `operational-distribution` gaps for all three Fresh laws, because nominal
  law references do not denote distributions;
- `operational-denotation` gaps for the Check, three evaluated guards, and two
  Reductions;
- `module-effect-denotation` gaps for all three semantic-module transitions;
- `oracle-carrier-representation` gaps for FullCanonical, PublicBinding, and
  LogicalAccess provider carriers and lookup behavior; and
- one `operational-outcome-map` gap for the provider image of all completion
  and noncompletion lanes.

The sourced coordinates are the Core identity; all three Challenge occurrence,
scope, type, domain, Fresh-law-reference, and public-condition leaves; the
independent/joint correlation leaves; shared/exclusive reduction-use leaves;
and all four Challenge-to-Reduction consumer backlinks. These facts are enough
to reject the required Challenge mutations, but not enough to execute the
interaction.

The five `property-premise` gaps are the three Claim predicates and the two
Reduction obligations. They remain outside the operational aggregate because
an owner-view coordinate can name a Claim or Reduction without proving it.

## 5. Consequence and non-claims

The immediate handoff is an evidence-package obligation, not a new semantic
owner: any later Q2 provider-correspondence attempt must first consume exact
integrated bodies issued by the existing five Core-view owners and the one
Protocol-view owner, or explicitly narrow its source contract to
`PublicCoinView`. The B5B2 fixture cannot fill that obligation for D1.

The checker cold-projects all four D1 neighbours and confirms that they remain
ineligible. The logical-reject neighbour supplies the fallback-Accept mutation;
the baseline itself has a fallback Reject. The neighbours are not emitted as
four further interactions, and none supplies the missing five projections, so
they cannot change this source-coordinate result.

This finite result establishes no provider/PIR correspondence, runtime or
replay correctness, module-host correspondence, Claim or Reduction truth,
protocol soundness or completeness, Fiat--Shamir theorem applicability or
security, ROM/QROM result, live compiler/backend behavior, or target migration.
The Lean term is parametric syntax. A sourced coordinate establishes only that
the stated rendering rule has an exact D1 view leaf to read.
