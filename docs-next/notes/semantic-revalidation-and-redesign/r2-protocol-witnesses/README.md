# R2 Real-Protocol Witness Program

> **Kind:** Temporary executable-witness charter and index
> **State:** Active R2 program; repaired `FRI-Grind-1` is closed and the
> differentiated protocol-expressibility portfolio is selected; `P01`
> Schnorr/Sigma has not begun
> **Authority:** None. A witness can falsify a candidate or recover a
> requirement; it cannot ratify target semantics, a theorem, implementation
> conformance, or migration policy.
> **Current witness:** Repaired `FRI-Grind-1`, from
> `test/Family/Inputs/frigrind.json`, completed its repaired refreeze/replay
> gate with explicit unexercised obligations
> **Downstream boundary:** Stage 4B remains inactive.
> **Deletion:** Absorb reviewed results into their durable owners, route open
> questions forward, then delete this package.

## 1. Purpose and evidence boundary

R2 subjects R1 requirements and competing protocol architectures to finite,
repository-grounded execution before selecting a replacement model. The probe
must expose semantic facts rather than reproduce current MLIR spelling.
Current code, specifications, fixtures, and generated artifacts are comparative
evidence only.

`FRI-Grind-1` pressures Statement binding, one fixed witness-local strategy
family with checked read/preview order, public-coin eligibility, Fresh and
Fiat--Shamir constructions, transcript framing, grinding placement, typed
routing, and relation/statement correspondence. It does not quantify an
adversarial strategy class and is not a complete FRI verifier. The recovered
execution ends at
`fri-terminal-not-modeled`: the source has no authenticated opening, Merkle
verification, fold equation, final-degree check, or `Accept`/`Reject` terminal.

The former local “authenticated opening” completion is withdrawn. It encoded
its own success condition and therefore supplied self-fulfilling evidence. R2
executable evidence ends at the source residual and makes no `Accept`, FRI
correctness, proximity, or completeness claim. A faithful authenticated FRI
witness requires a later native oracle/IOR case.

The external Fresh artifact supplies one exact support point with no
`ExecutionRecord` dependency. This is a provenance result only: it establishes
neither uniform sampling, stochastic independence, nonce-before-tape authoring,
nor a general non-anticipation property.

Frozen inputs, reconstruction, and primary-source deductions live in
[Source Reconstruction and Primary Research](source-and-research.md). Replay
instructions and the machine-report contract live in the
[clean-room probe README](../../../../evaluation/r2-protocol-model/README.md).
The failed first refreeze, accepted repair boundaries, and passing repaired
refreeze are recorded in
[FRI-Grind-1 Cold Review and Repair Decision](cold-review-and-repair.md).
The next cases, validation depths, recent variants, holdouts, ordering, and
promotion rules are fixed by the
[ZK-Protocol Expressibility Validation Portfolio](expressibility-validation-portfolio.md).

## 2. Method

The work order is:

1. reconstruct source facts and classify each as recovered, theorem premise,
   convention, local choice, or open;
2. close the minimum typed semantics demanded by the witness;
3. run well-formed negative mutations at named semantic boundaries;
4. compare Fresh/FS subjects and relation strengths without conflating them;
5. pressure identity and Relations bridges; and
6. freeze decisive results, failed gates, and the next materially different
   witness.

The probe must not import the current MLIR dialect, `SealBattery`, canonical
encoder, Python oracle, generated vocabulary objects, or target-document data
classes. It may parse a frozen fixture and construct a separately declared
finite scenario whose correspondence to that fixture is checked. Matching
current output is neither necessary nor sufficient.

## 3. Required pressure

The typed kernel must represent Statement occurrences, private/public inputs,
actor visibility, strategies, transcript influence and framing, Fresh and FS
realizations, deterministic verifier actions, public-coin eligibility, closed
codecs/samplers/algorithms/regimes, typed checks and reductions, and
source/anchor provenance. Admission, execution, relation correspondence,
theorem applicability, and quantitative Analysis remain different judgments.
The finite probe checks only fixed strategy contracts against Core order.
Durable semantics must expose execution as a relation parameterized by
protocol, strategy, inputs, and verifier coins; Analysis must quantify over an
admitted causal or adversarial strategy class.

Required mutations cover: omitted, delayed, duplicated, wrongly encoded, or
substituted Statement influence; Wire-only `g1` or nonce; challenge-namespace
collision; verifier-private influence; future reads by `g1`; altered routing;
unsupported or missing algorithms; resource exhaustion; lossy projection
misclassified as an isomorphism/embedding or priced without source and
occurrence premises; undeclared source reads; and prover-controlled absorption
after grinding but before protected query randomness. Each case must be
well-formed until its intended first rejection boundary.

## 4. Candidate comparison and reopened requirements

R2 compares two orthogonal axes. The subject axis asks about one shared
subject, mapped distinct subjects, or a shared interaction skeleton with
distinct Fresh/FS realizations. The relation axis separately asks for typed
structural maps, challenge-parametric interaction commutation, observation
projection, strategy equivalence, and any probabilistic or cryptographic
transport.

Grinding makes an unqualified Fresh/FS equality claim inadmissible in this
witness. The fixed FS strategy may preview transcript-derived pow values while
searching; the fixed Fresh strategy sends its nonce before later coin reveal and
declares no future-coin reads. The external Fresh tape is only one non-FS-derived
support point, so this comparison establishes neither distributional separation
nor a general non-anticipation theorem.

The current redesign must satisfy all of the following before any candidate is
counted:

- `ExecutionRequestId`, not `InvocationPackageId`, closes over the scenario,
  inputs, semantic application context, evaluator basis, resources,
  realization-local controls, `CoreDerivationKind`, source fixture, and source
  package;
- Fresh owns Core-derived coin slots and declared sequential-reveal,
  conditional-uniformity, and no-future-access laws. A concrete tape
  instantiates one support point; tape qualification does not establish its
  distribution law;
- a hybrid has separately identified skeleton and realization objects plus
  checked projection/composition laws; a hash inferred from two already-built
  records is not a factorization;
- a one-point value check is called pointwise correspondence, never a
  whole-domain embedding or isomorphism; and
- reports use a compact identity graph with bounded root identities rather
  than stable "sections" or a second serialization of traces and schemas.
  The report identity closes over those roots, and canonical replay recovers
  the full typed subjects and judgments.

This witness supports exactly one explicit `SemanticRegimeId`. Its local closed
finite-term ABI contains null, booleans, integers, UTF-8 text, bytes, canonical
sequences, and sorted string-keyed maps, with tagged length-delimited encoding
and explicit byte, node, and depth bounds. Every semantic identity binds that
regime, an ASCII domain, and the encoded preimage; unsupported regimes are
rejected. This is not a proposed universal v0 ABI.

These conditions and context-isolated replay now pass for the repaired witness.
Shared, distinct, and hybrid results are retained only at their exact finite
strength; a future failed hybrid remains open rather than being promoted by
nomenclature.

## 5. Gates

| Gate | Required evidence |
|---|---|
| Source fidelity | Frozen fixture/package identities, exact `InputBundle` grounding, explicit `CoreDerivationKind`, and explicit exclusions |
| Execution closure | Qualified FS source-residual, external non-FS-derived Fresh support-point, and FS-coupled Fresh executions with exact request identity and dependencies |
| Negative quality | Every named mutation reaches its intended first semantic rejection |
| Candidate neutrality | Two nontrivial subjects at equal resolution; hybrid counted only after genuine factorization |
| Identity closure | Explicit supported regime, closed finite-term ABI, full-domain codecs, and bounded evaluation for every identity-bearing input and algorithm |
| Resource closure | Request execution and target-plus-dependency qualification replay are separately pre-admitted, bounded, and accounted |
| Relations/loss closure | Every bridge classified; retained loss has source meaning, occurrence accounting, game, and transform |
| Non-authoring replay | A context-isolated evaluator reproduces normalized classifications from frozen public inputs |

Failure is a valid R2 result and must retain its exact inputs and reopening
conditions. Later work follows the differentiated portfolio rather than making
every case executable: T1 boundary analyses, T2 constructive encodings, and T3
executable witnesses carry different evidence. `P01` Schnorr/Sigma is the next
case; native FRI/IOR is the second required T3 anchor. Recent design-set
variants and withheld cold cases prevent the portfolio from collapsing into a
list of canonical examples.

## 6. Status and non-claims

R2 remains active, but the repaired `FRI-Grind-1` attempt is closed. The v4
oracle reproduces 41 subject-and-evidence-bound classifications; all 39 local
tests pass; and a minimal copied checkout loaded its own evaluator modules,
reproduced the same report, passed the same suite, and rejected an alternate
root. The first self-fulfilling completion and first failed refreeze remain
retained as negative evidence rather than being overwritten.

Closure is not an all-green semantic verdict: relation-side Statement
qualification remains `MissingDependency`, while substituted Statement
influence and Fresh distribution evidence remain `NotExercised`. These
classifications are frozen outputs and requirements for later witnesses, not
silently waived gate pressure.

This closes only the finite FRI-grinding pressure already named by this
witness. The next R2 case must add materially different semantics rather than
more local variants of the same trace. The broader candidate and holdout set is
selected by the adjacent portfolio, but none of those protocol cases has been
validated merely by being listed.

R2 does not establish cryptographic security, theorem applicability, protocol
soundness or completeness, outer-relation correctness, compiler correctness,
MLIR suitability, implementation conformance, performance, migration
feasibility, property transport, verifier-coin uniformity or independence,
artifact-authoring order, adversarial non-anticipation, event probabilities, or
a final v0 architecture. A finite model is structural falsification evidence
only.
