# R1 Invariant Ledger

> **Document kind:** Temporary cross-domain requirement ledger
> **Document state:** Active
> **Owner:** `project`, routing each invariant to its semantic owner
> **Authority:** None. These are recovered requirements and falsification
> targets, not a selected representation or normative specification.
> **Source:** The independently adjudicated findings in
> [review-adjudication.md](review-adjudication.md).
> **Disposition:** Resolve each item through R2--R4, absorb it into one durable
> owner or an explicit extension boundary, then delete this ledger.

## 1. How to use this ledger

An invariant constrains every viable candidate without prescribing its data
structure. A candidate does not satisfy an invariant by repeating its name: it
must identify the owner, formation and checking law, positive inhabitant,
well-formed negative mutation, resource behavior, and downstream consumer.

`Required` means every viable v0 candidate must close the item. `Preserve`
means the current target has an important semantic hypothesis that remains the
leading candidate, although its representation and even its final retention
remain subject to the stated executable revalidation. It is not a universal,
solution-independent law. `OpenScope` means R2/R3 must first decide what v0
promises.

## 2. Foundation and identity invariants

| ID | State | Invariant | Acceptance pressure and owner |
|---|---|---|---|
| F-01 | `Required` | **Regime closure.** Every semantic regime has exact identity derivation, meaning, support, equality, and compatibility rules. A first-class acquired regime additionally needs authentication, admission, and lifecycle laws; a static regime needs an exact version/change law, not a fictitious runtime rotation path. | Two independent implementations must derive the same identity/support result and reject an unsupported or ambiguously interpreted regime. Foundation owns only the common mechanism; each domain owns regime content and whether the static or first-class lane applies. |
| F-02 | `Required` | **Canonical boundary-value closure.** Every value that crosses identity, transport, equality, or execution boundaries is domain-indexed and has decidable membership, unique canonical bytes/equality, decode rejection, and substitution rules. | A noncanonical encoding, wrong domain, or ambiguous equality must fail before the affected boundary. Project owns the cross-domain condition; exact domains own membership, bytes, and equality; Foundation owns only a common mechanism that passes extraction. |
| F-03 | `Required` | **Algorithm closure.** Every identity-bearing algorithm has an exact kind-indexed ABI, canonical preimage or referenced contract, deterministic transition/evaluation semantics, and named failure behavior. | Round-trip a real codec/sampler/bridge; reject wrong arity, domain, state, or result. Shared envelope only if extraction proves identity; semantics remain domain-owned. |
| F-04 | `Required` | **Totality has evidence.** A totality claim names the proposition, evaluator, domain, resource model, evidence form, checker/trust root, and identity effect. | A mere declaration cannot establish totality. Include a malformed, partial, or exhausted evaluator case. |
| F-05 | `Required` | **Bounded identity work.** Identity preimages and admission-derived work are profile-bounded, certificate-bounded, or terminate with a nonsemantic checker/resource failure. | Measure stored bytes, peak derived structure, memory, and work on real and adversarial guards. No divergent semantic verdict. |
| F-06 | `Required` | **Identity-field justification.** A field or collection order enters identity only if it changes an authorized observation, creates a necessary stable reference namespace, or enforces a stated domain-separation invariant. | Remove or justify one semantically inert permutation/field; demonstrate the consumer or attack boundary for every retained one. Domain owner plus identity specification. |
| F-07 | `Preserve` | **Acyclic self-binding.** A self-derived value has an exact construction whose identity dependencies are acyclic and whose post-ID interpretation is unique; no evidence or policy can reintroduce a hash cycle. | Independently evaluate the current `BindConstructionSelfId` inhabitant and at least one equally exact alternative if available; reject premature resolution and recursive regime/policy roots. |
| F-08 | `Required` | **Kind-safe ABI algebra.** Algorithm, capability, proof/interface, relation bridge, and endpoint ABIs carry enough kind information and explicit compatibility judgments to reject cross-kind substitution. | Every bare “exact ABI” reference must route to one schema owner. Either disjoint schemas or one dependently indexed algebra may satisfy the requirement. |

## 3. Protocol, transcript, and execution invariants

| ID | State | Invariant | Acceptance pressure and owner |
|---|---|---|---|
| P-01 | `Required` | **Complete instance binding.** Every Statement occurrence in a challenge's declared scope has explicit canonical, typed, injective influence on the challenge state before that challenge, with exact scope, ordering, and multiplicity rules. | Omit, substitute, reorder where order is semantic, delay, or use the wrong codec for one Statement occurrence; duplicate only where the selected profile forbids it. PIR invocation and FS construction. |
| P-02 | `Required` | **Derived transcript obligation.** Required challenge sources follow from statement purpose, proof/round/oracle role, and exact contracts, not solely from an authored Transcript observation bit. | A required Proof message changed to Wire-only rejects, while a declared irrelevant application message remains admissible. PIR and exact round/oracle contracts. |
| P-03 | `Required` | **Exact prefix closure.** Every active required source precedes the challenge and appears in the exact active ordered prefix with its declared occurrence/multiplicity rule. | Reproduce the semantic boundaries behind E211, E212, and E214 without importing their current representation by fiat. Namespace collision and reuse are tested under P-07. PIR. |
| P-04 | `Required` | **Explicit relaxation.** Omitting a normally required source is legal only under a named theorem/profile with exact assumptions, scope, and quantitative consequence. The selected K2 construction deliberately permits no such omission: it absorbs every active prior prover publication. A relaxation requires a separately identified checked construction rather than an Analysis result that silently weakens K2 admission. | A weak/static profile cannot be consumed as strong/adaptive FS. Exercise a distinct theorem-priced construction or reopen the FS cone if a real protocol requires one; PIR owns structural construction, while Analysis owns applicability and loss. |
| P-05 | `Required` | **Public-coin eligibility.** Every verifier action in the transformed proof interaction is a deterministic function of public history and explicit PublicEnvironment randomness; no verifier-private value or hidden state influences a prover-visible move. | Introduce a verifier-private invocation value, private sample, hidden state dependency, or unlinked stochastic verifier move and require admission to reject it. PIR Core/FS admission. |
| P-06 | `OpenScope` | **Declared FS strength.** If `FiatShamir` denotes structurally strong FS, every proof-interaction verifier message must be challenge-derived, publicly reconstructible, or explicitly outside the transformed interaction. If it denotes a generic transcript-derived interpretation, strong-FS eligibility must be a separate checked result. | Encode a deterministic nonchallenge verifier message and decide which result admits or rejects it; no message silently disappears under either interpretation. PIR. |
| P-07 | `Required` | **Unambiguous challenge derivation.** The squeeze/sample ABI includes a collision-free, composition-safe occurrence namespace and a full `(state, conditional inputs) -> (state', value or named failure)` relation. It need not be one globally unique authored string. | Ambiguous/reused occurrence namespace, wrong state transition, and grinding/rejection failure mutations, including the semantic boundary behind E216. PIR FS construction. |
| P-08 | `Required` | **Causal prover generation.** A prover move depends only on its visible past, private inputs/witness, prior private state/randomness, advice, and permitted oracle responses. | A commitment depending on a future challenge is not strategy-generated even if a whole serialized trace replays. PIR exposes decision points/history/legal moves; Analysis owns strategy classes. |
| P-09 | `Required` | **Causal reproducibility and auditability.** Strategy execution exposes enough authenticated history to reproduce or independently audit the induced outcome distribution under the same invocation and randomness. | A paired online/replay semantics or one evaluator that emits a checked trace may satisfy the requirement; exercise honest and malicious strategies and reject a clairvoyant trace as non-generated. PIR/Analysis seam. |
| P-10 | `Required` | **Typed claim routing.** Claim production, reduction inputs/outputs and side inputs, and check operands satisfy explicit contract/parameter/domain equations. | Wrong claim contract, parameter order/domain, reduction output, or check input rejects. PIR. |
| P-11 | `Required` | **View adequacy is checked.** A source-owned finite view covers the exact consumer-owned read closure, with defined missing, extra, selector, and equality behavior. | Remove one required fact, add a forbidden authoritative mirror, or request an unknown question. Source domain plus consumer. |
| P-12 | `Preserve` | **Checked Fresh/FS factorization.** Fresh and FS Protocols are connected by an explicit checked construction that closes all interpretation-specific obligations. Sharing one admitted Core is the current favored hypothesis, not part of the invariant itself. | Compare a genuine same-Core FRI pair with a related-distinct-Core or bisimulation-style challenger and apply negative construction mutations. PIR. |

## 4. Relations invariants

| ID | State | Invariant | Acceptance pressure and owner |
|---|---|---|---|
| R-01 | `Required` | **Correspondence-regime reachability.** Every operation that requires an admitted regime has a constructible static or first-class regime path, exact authority, and cold replay. | Reconstruct the regime and every dependency from persistent inputs; reject a substituted regime. Foundation mechanism plus Relations specialization. |
| R-02 | `Required` | **Closed artifact-fact algebra.** Fact schemas, typed facts, field references, selectors, absence/multiplicity, equality, and profile/adapter compatibility are formed and canonically encoded. | Interpret one real R1CS/AIR-style artifact and reject wrong-schema selectors and ambiguous fields. Relations. |
| R-03 | `Required` | **Explicit grounding equations.** Committed-object grounding names every typed left/right operand, source, equality relation, evaluation order, and failure fact. | One affirmative real object plus wrong commitment, material, selector, position, and equation mutations. Relations; Analysis owns later cryptographic faithfulness. |
| R-04 | `Preserve` | **Lossless bridge laws are explicit.** Whole-domain semantic equivalence is a total bijection with both inverse laws. A lossless embedding is injective, has an exact image predicate, and is invertible on that image. Neither may be represented as a lossy projection. | Round-trip a full equivalence in both directions, round-trip an embedding on its image, and reject a lossy mapping presented as either. Relations. |
| R-05 | `Required` | **Every retained lossy projection is separate and priced.** A directional identity/transcript-material projection names authenticated source meaning, source and target domains, canonical algorithm, collision relation/game, any preimage premise, exact occurrences, and a quantitative loss transform. | Model the current 256-to-216-bit anchor case with a grounded reduction or deliberately eliminate it, for example by retaining a full digest. Relations owns projection meaning, PIR owns absorb occurrences, and Analysis owns the game and loss. |
| R-06 | `Required` | **Relation-facing witness surface.** PIR explicitly distinguishes relation Witness occurrences from confidential Context, ProtocolValue, and internal obligation outputs. | A protocol-private nonwitness input must not force a Relations witness port; an exported derived witness must be expressible. PIR defines surface; Relations compares it. |
| R-07 | `Required` | **Result correspondence has a direction.** Structural result references, relation acceptance, Protocol acceptance, soundness direction, completeness direction, and full equivalence are distinct judgments. | A structural binding cannot be consumed as behavioral equivalence. Relations/PIR, with universal theorems in Analysis. |
| R-08 | `Required` | **Binding questions retain meaningful negatives.** Admission, structural correspondence, instance correspondence, grounding, and behavioral correspondence do not duplicate the same predicate under stronger names. | For every affirmative result, exhibit a well-formed admitted operand that can produce its declared Negative, or derive the fact internally instead. Relations. |

## 5. Analysis and Compiler invariants

| ID | State | Invariant | Acceptance pressure and owner |
|---|---|---|---|
| A-01 | `Required` | **Theorem grounding.** A property result names its exact game/experiment, causal strategy class, model/oracles, resource regime, assumptions, theorem or checked derivation, correspondence premises, and loss. | Instantiate at least one real FS theorem and reject an assumption/model mismatch. Analysis. |
| A-02 | `Required` | **Quantitative-loss completeness.** Every approximation, projection, reduction, composition, or transform loss has an exact occurrence/accounting source and enters one typed ledger. | The anchor projection and one FS/composition loss must be recomputed without hidden terms. Analysis. |
| A-03 | `Preserve` | **Property-family separation.** Soundness does not imply round-by-round soundness, ROM does not imply QROM, and HVZK does not imply malicious-verifier ZK without an exact checked transport. | Wrong-family or wrong-model capability use rejects. Analysis. |
| A-04 | `Preserve` | **Cost meaning precedes optimization.** The producer owns raw observation meaning and completeness; Evidence owns provenance, procedure, environment, samples, and uncertainty; Analysis owns derived cost judgments and inference; Compiler owns comparison under explicit objectives and comparators. | A measured estimate cannot masquerade as exact semantic cost, incomplete observations cannot acquire false completeness, and missing objective data cannot become an implicit score. |
| A-05 | `Required` | **Primary-source traceability.** Source-specific cryptographic notions and theorem instances have maintained primary citations, exact versions, scope, and non-transferable assumptions. | A cold reader can recover the theorem statement and see why its hypotheses match; citation alone never grants authority. Analysis/project related work. |

## 6. Cross-domain and process invariants

| ID | State | Invariant | Acceptance pressure and owner |
|---|---|---|---|
| X-01 | `Required` | **Nonvacuous semantic relations.** Every semantic affirmative/negative relation states the exact postcondition and admits a meaningful well-formed negative. Deterministic derivation or conformance checks may instead report typed mismatch, malformed input, or invalid evidence. | Do not manufacture a semantic Negative by corrupting a deterministic constructor's only canonical output; test the declared failure class at each relation boundary. |
| X-02 | `Required` | **Derived evidence remains derived.** Canonical maps, prefixes, endpoint obligations, and indexes are recomputed or accepted only through an independently checked certificate/proof or authenticated derived cache; they are never elevated into authored semantic truth unless they carry distinct choice. | Two independent routes reproduce or validate the same derived object; mutation is detected without turning the cache or certificate into the semantic owner. |
| X-03 | `Required` | **Outcome and failure separation.** Semantic Negative, unsupported question, malformed input, missing dependency, resource exhaustion, and checker failure remain distinct and cannot be cached or consumed interchangeably. | Exercise one case of each at a real boundary. Foundation envelope plus domain owners. |
| X-04 | `Required` | **Durable semantic closure.** A durable page depends only on durable owners or explicit inactive extension boundaries; temporary notes never supply a required type or law at cutover. | Delete or hide the notes tree and re-run link, symbol-owner, and clean-room reconstruction checks. Project governance. |
| X-05 | `Required` | **Falsifiable selection.** No target contract is selected until it names a falsifier and that falsifier has been run. A falsifier is a concrete executable observation naming an input, an object, and the outcome that would be witnessed if the selection were wrong; it is not a restatement of the selection's derivation and not a task. It must measure the claimed property itself, not an adjacent artifact reported in its place. A falsifier whose failing outcome no reachable state of the system can produce — or which fails only on malformed input — does not count and is struck. | Each row is tiered. `Uncontested`: the rejection of each rival is not disputed, and the row closes on the run falsifier plus a costed rejection of each rival; no rival is built. `Contested`: the rejection is genuinely disputed — an external counterpart or a downstream consumer needs the rival, or the system already realizes the rival's property in some component, so the inconsistency is internal. A contested row additionally owes the rival built **and** both alternatives carried on a shared substrate, one where a single input drives both and the falsifier's outcome can differ between them; inhabitation on separate substrates closes nothing. A row is contested until its rivals' rejection is shown undisputed, and moves back only on that showing. Project method. |
| X-06 | `Required` | **Independent gate evidence.** A closing gate freezes inputs, distinguishes author and falsifier roles, permits a failed artifact, and records exact reopening conditions. | External validator can rebut an adjudication without accepting its proposed repair. Project method. |
| X-07 | `Required` | **One semantic owner after convergence.** Temporary targets, matrices, and narratives are absorbed or deleted; shared mechanisms are defined once without moving domain meaning into Foundation. | Shadow-definition scan, manifest closure, and cold-reader reconstruction after R5. Project governance. |

## 7. Target-owes-shipped non-regression baseline

Here, **shipped** means the current authoritative semantic contract under
`docs/`, with the current implementation and tests used as correspondence
evidence and counterexample sources. A known implementation defect does not
weaken the contract the target owes. Conversely, preserving an implementation
accident is not a design requirement.

This baseline is revision-pinned to pre-K0 commit
`d6868a447d5bedb86c017bf8a35adf3b480e9a7c`: `docs/` at that revision owns the
semantic debt, while `include/`, `lib/`, `test/`, and `evaluation/` at the same
revision supply implementation correspondence and counterexamples only. A
later current-system change does not silently move this baseline; rebasing it
requires an explicit K0 amendment with changed rows and rerun falsifiers.

The comparison has three tiers:

1. On their shared domain, target semantic guarantees must be at least as
   strong as the current contract. A deliberate removal requires its own
   selected decision, affected-consumer analysis, and executable falsifier; it
   cannot occur by omission.
2. Carrier, encoding, and evolution choices may change incompatibly, but their
   canonicity, refusal behavior, resource use, and independent reproducibility
   must be measured as equal or better against frozen inputs.
3. New capabilities are not free. Each records the extra identities,
   declarations, checkers, failure surface, representation cost, and consumers
   that purchase it.

No row closes because a target page contains a similarly named type. The named
falsifier must run against a target-native object and reach the boundary whose
guarantee is being compared. Every falsifier invocation includes an unchanged
positive control at the same boundary so that a checker which rejects
everything cannot satisfy the row.

### 7.1 Semantic guarantees

| ID | Current contract and target debt | State and owner | Executable falsifier |
|---|---|---|---|
| S-00 | **Fail-closed authentication and admission.** Raw or decoded objects are rechecked against exact immutable dependencies and identity; caller assertions, cached booleans, receipts, and mutable handles confer no authority. K1 now specifies and executes bounded identity, regime, value, portable-function ABI, typed-failure, and totality mechanics. It does not execute the complete raw-object lifecycle: full carrier reauthentication, capability non-transfer, and exact PIR candidate admission and readmission remain open. | `Foundation mechanism bounded-complete; lifecycle and PIR integration open`; Foundation owns the selected substrate and PIR owns domain admission. | Serialize an admitted target object, mutate one identity-bearing dependency or the claimed ID, and present the old capability or a cached affirmative. Any consumer acceptance without complete reauthentication and readmission falsifies the target. |
| J1 | **Structural Fiat--Shamir admissibility.** The current BIND judgment derives requirements from round contracts and structurally rejects omitted statements, late or unabsorbed required proof material, Last-Challenge/Frozen-Heart shapes, and colliding challenge domains. The narrowly reclosed K2 target now defines scoped Statement initialization, dependency-computed public-coin eligibility, exact state-passing squeeze/sample behavior, derived transcript actions, and exact full-prefix equality. Ordered required influence is a derived audit projection of that prefix, not a second independent runtime gate. K2 is deliberately stricter than the current theorem-priced early-unabsorbed-slot relaxation. | `Bounded structural regression, correspondence, and one structural theorem-applicability profile closed`; PIR owns structural admission and any separately identified relaxed construction, Relations owns external Statement completeness, and Analysis owns theorem applicability and quantitative price. K3-C closes only the selected AFK applicability contract; it mints no all-`n` family source capability and therefore no family transport result absent independent proof authority. Broader profiles and any theorem-priced structural relaxation remain open. | Run one target-native fixture and well-formed mutations that omit or delay a scoped Statement, mark required proof material nontranscript, move a required message after its challenge, reuse a challenge occurrence namespace, and add verifier-private influence. The positive set must include a multi-segment construction whose later segment binds its Statement after an earlier segment's challenge but before its own first challenge. Acceptance of any mutation or rejection of either control falsifies the target. A justified relaxation must identify a different checked construction; silently weakening the K2 result also falsifies the target. |
| J2 | **Linear claim accounting.** Every claim has exact production and consumption, the claim graph is acyclic, and duplication, weakening, split, batch, fold, and sink behavior are explicit and contract-gated. | `Parity unproved`; PIR claim/resource algebra. | From one admitted multi-claim fixture, drop a consumption, duplicate a use, create a cycle, and change a declared multiplicity. Any admitted mutation or refused unchanged control falsifies parity. |
| J3 | **Canonical identity, equivalence, and diff.** Identity covers every meaning-bearing field and cited dependency but excludes nonsemantic evidence, backend metadata, and recomputable caches. An independently identified derived semantic subject instead has its own identity and exact source binding. Consumers can independently recompute both. | `Open closure`; Foundation identity mechanism plus PIR semantic and carrier owners. | Two independent evaluators decode the same carrier and derive identical semantic IDs and field-level diffs; mutate each semantic field and each excluded metadata/cache field. A semantic mutation preserving identity, an excluded mutation changing it, an independently identified derived subject losing its source binding, or evaluator disagreement falsifies the target. |
| J4 | **Checked transformation legality.** A target's admission does not establish its relation to a predecessor; the current Compiler re-runs family recognition, realization, relation checks, quantitative ceilings, and obligation non-growth. | `Downstream parity debt`; Compiler owns selection, the exact transform family owns the checked predecessor/successor relation, and PIR exports authenticated operands. | Hold admitted predecessor and successor fixed, then mutate the transform trace, family checker result, quantitative ceiling, or obligation comparison. A checked decision that survives any mismatched relation input falsifies the target; target admission alone must remain unaffected. |
| J5 | **Source-relative endpoint preservation and non-vacuity.** Seal derives every source projection obligation and projection proves exact coverage with no omission or phantom effect; local endpoint validity is insufficient, and a verifier face with no accepting check cannot become an accept-all endpoint. The current `COV_obl` implementation gap recorded as CUR-8 does not waive this normative debt. | `Open read seam`; PIR owns source obligations and OIR owns source-relative projection. | Project verifier and prover endpoints, then independently drop one required effect, add one phantom provenance position, use a wrong discharge family, and remove the verifier's check. Any affirmative source-relative projection for a mutation falsifies the target. |
| J6 | **Composition and descent.** Composition rechecks the combined subject, imported requirements, joint transcript state, namespace separation, claims, and residual obligations rather than inheriting child admission. | `Parity unproved`; PIR composition with relation-specific or OIR-specific descent owned by the result domain. | Compose two independently admitted children, then reset shared transcript state, collide namespaces, leave an imported requirement unresolved, or misroute one claim/obligation. An admitted composite or affirmative descent result for a mutation falsifies the target. |
| J7 | **Post-admission conditional security derivation.** Security is a separate, typed derivation over authenticated structural facts, explicit rules, models, hypotheses, bindings, and quantitative terms; admission or a citation alone proves none. | `Bounded K3-C three-seam contract and refusal grounding closed`; Analysis owns property meaning and PIR/Relations own the exact native source views. The finite native Schnorr judgment, abstract-family AFK transport, and pointwise family/member specialization are distinct propositions. Family transport requires an independently established uniform all-`n` source capability plus theorem truth; K3-C defines no native rule that mints that capability, so the transport is `CannotAnswer` absent external proof authority. | Reproduce the finite native judgment and the family/pointwise formation boundaries, then alter the theorem binding, model, hypothesis, structural source, family capability, correspondence, or one quantitative occurrence. An unchanged result, an affirmative family transport without exact external all-`n` source authority, use of the finite judgment in that source slot, reverse generalization through the pointwise seam, or any property minted directly by Protocol admission falsifies the target. |
| J8 | **Exact local reduction closure.** Reduction inputs, outputs, round structure, checks, attachments, material equations, and constructed claim descriptors agree with one exact contract. | `Open closure`; PIR contract and claim-routing algebra. | Mutate one input/output profile, role-selected check, round assignment, material attachment, or output constructor in a well-formed reduction fixture. Any admitted mutation falsifies the target. |
| J9 | **Typed terminal closure.** Every discharged claim reaches one exact check-backed terminal rule with total roles, typed attachments, permitted reuse, and complete material-binding use. | `Open closure`; PIR terminal, failure, and material-binding algebra. | Mutate the consumed claim pattern, terminal role map, selected check contract, attachment relation, reuse, or binding coverage. Any admitted mutation falsifies the target. |
| S-10 | **The bounded imported-verification carrier shape is not silently erased.** The current identity-bearing `artifact_verify` records only a bounded subset of future recursion/zkVM facts, and both endpoints deliberately refuse it; it does not verify a child artifact. The target owes preserved protected-source meaning or an explicit unsupported extension boundary, not successful recursive verification. | `Open target-home/evolution selection`; PIR owns the source event/effect and OIR owns typed projection refusal. | Encode the current bounded fact shape target-natively and mutate each identity-bearing fact. Failure to represent the control without opaque escape, a mutation preserving identity, or projection producing a partial OIR instead of typed `Unsupported` falsifies the target. |
| S-11 | **Decidable bounded structural admission on the shared domain.** Current supported structural checks terminate over finite inputs with a fixed descriptor-depth counter, finite well-founded dependency closure, and fail-closed typed refusal. The target owes that behavior on the shared domain. A uniform resource envelope for new guards, codecs, samplers, and algorithms is a K1 selection and new-capability price, not a shipped guarantee. | `Open shared-domain parity`; PIR admission, using Foundation's selected finite-term mechanism. | Admit one valid current-shape object at the supported depth boundary, then exceed the descriptor-depth bound and present cyclic, unresolved, unknown, and malformed dependencies or constructors. Divergence, authority after the depth/closure boundary fails, an untyped failure, or refusal of the control falsifies parity. New target-only structures are tested separately by M-03 and N-04. |
| S-12 | **Material-reference integrity.** Stable semantic material references and verifier-visible local values are partial-function, reverse-injective, consumed bindings; composition may reindex local values but cannot alias or silently drift stable material. This proves reference equality only, not witness truth. | `Open target definition`; PIR owns material identity and binding, with Relations owning any additional value proposition. | Bind two values to one stable reference, bind one value twice, leave a retained edge unused, and create a cross-child alias. Any admitted mutation, or refusal of the one-to-one consumed control, falsifies the target. |
| S-13 | **Typed prover-route totality and linear handle use.** Within the supported construction profiles, every required proof slot has exactly one typed route or explicit hole contract, dependencies are acyclic and available before use, and opaque handles have one consumer. This is structural construction, not completeness. | `Open downstream parity`; PIR Plan owns source routing and OIR owns endpoint realization. | Remove or duplicate a slot route, change a hole ABI/class/count, create a future dependency or cycle, and duplicate a handle reader. Any accepted mutation, or rejection of the fully routed control, falsifies the target. |
| S-14 | **Relation fact authority and explicit information loss.** Relation interface admission, artifact interpretation, checked comparison, Protocol correspondence, and satisfaction remain distinct authorities. Any retained non-bijective bridge is named, directional, occurrence-bound, and quantitatively priced or explicitly left as an obligation; no digest equality silently becomes predicate truth. | `Bounded authority separation and typed loss-import seam closed; concrete loss open`; Relations owns the distinctions and bridge meaning, PIR supplies authenticated source views, and Analysis owns quantitative consequence. K3-C does not discharge the concrete `sha256-216` reduction or price. | Substitute a raw observation for a checked prerequisite, use structural correspondence as satisfaction, or treat a lossy projection as bijection with zero loss. Acceptance of a mutation, or inability to express the valid separated control, falsifies the target. |

The authoritative source map for J1--J9 is the current
[Kernel purpose table](../../../docs/spec/kernel.md#0-purpose). Exact present
rules remain in their owning current specifications; this table is a regression
index, not a second definition. Current implementation defects remain separately
routed by [R1 adjudication](review-adjudication.md#8-current-system-findings-checked-during-review).

### 7.2 Representation and evolution measurements

| ID | Selection being compared | Owner | Executable falsifier |
|---|---|---|---|
| M-01 | Current MLIR SSA/token references versus target canonical ordinals and semantic references. | PIR semantic/carrier correspondence. | Round-trip one multi-round, multi-claim target through the abstract evaluator and MLIR carrier; inject stale, duplicated, reordered, and cross-kind references. A semantic mismatch, ambiguous canonical form, or weaker refusal falsifies the selected representation. |
| M-02 | Current canonical bytes, independent C++/Python twin parity, and depth-bounded encoding versus the new semantic/carrier identity split. Byte compatibility is not required. | Foundation encoding envelope and each identity owner. | Two independent implementations encode/decode the same frozen corpus and adversarial noncanonical inputs while recording bytes, size, and work. Disagreement, multiple accepted encodings, or an unbounded path falsifies the selection. |
| M-03 | Current deterministic diagnostics and bounded seal work versus target algorithm, guard, and admission work. | Exact semantic owner plus implementation-conformance Evidence. | Run real and adversarial guards, codecs, samplers, and dependency closures under declared limits. Divergent semantic verdicts under resource pressure, undeclared exponential work, or nondeterministic first-boundary classification falsifies the selection. |
| M-04 | Current fail-closed vocabulary growth versus a closed target algebra and regime evolution. | PIR vocabulary/evolution with Foundation regime mechanism. | Add one locally scoped event/effect through the declared extension lane and present one unknown constructor. Acceptance of the unknown, or forced rotation/change of unrelated identities when the extension is supported, falsifies the selected evolution model. |

### 7.3 New-capability price ledger

| ID | Capability and promised value | Required price and owner | Executable falsifier |
|---|---|---|---|
| N-01 | One `InteractiveCore` can support checked Fresh and multiple FS interpretations without duplicating interaction meaning. | Distinct construction/Protocol identities, construction checker, maps, and no hidden duplicated source declaration; PIR. | A Fresh/FS pair and second FS interpretation must share the declared Core facts while differing only where interpretation requires. If the views duplicate or disagree on round, Statement, or required-influence meaning, the factorization has not purchased its claim. |
| N-02 | Interfaces, Plans, and narrow consumer views vary independently of Protocol meaning. | Extra dependent identities, admission/checking, view adequacy, and bounded retained state; PIR and consuming owner. | Exhibit two useful Interfaces and two useful Plans for one Protocol and measure object/checker cost. If changing either alters a Protocol-only observation, or no independent consumer reads the distinction, the split must be reduced or deferred. |
| N-03 | Native oracle/IOP, recurrence, and additional protected-effect constructs can extend the finite kernel. | Exact operational laws, extension/evolution behavior, projection obligations, and protocol-family witnesses; PIR with OIR/Analysis consumers. | Native FRI/IOR and one holdout must use the construct without opaque escape. If local extension forces unrelated regime rotation or cannot reach a downstream read, the capability is not purchased. |
| N-04 | Canonical guarded schedules support concise conditional behavior. | Declared size/work bounds or certificates, deterministic failure, and no exponential object hidden in identity; PIR/Foundation resource boundary. | Measure a real guard corpus and adversarial variable orders. If admission crosses its bound without a nonsemantic resource failure, or equivalent guards receive unstable meaning, the representation is rejected. |
| N-05 | Federated Analysis and richer Compiler decisions support independent property families and producers. | Every additional subject, result, policy, replay form, and closed domain needs an independent consumer and a smaller-rival comparison; Analysis/Compiler, downstream of kernel freeze. | Before activation, two independent families or producers must require the distinction on one shared substrate. If the same observable decision is obtained from the smaller current-style contract, the extra surface remains notes-only. |

## 8. Minimum pre-freeze consumer questions

This freezes the **questions the kernel must answer**, not their final schema,
representation, or identity preimage. K1--K3 may choose a smaller or different
answer than the current target pages, but PIR cannot freeze while any question
requires a carrier escape, a shadow Protocol schema, or an ambient read.

| ID | Question PIR must make answerable | Answer owner and failing observation |
|---|---|---|
| REL-Q1 | What is the complete Statement-only public occurrence table, with domains and multiplicities, and how does an Interface bind external assignments to it? Mapped-occurrence correspondence and optional whole-Protocol Statement coverage must remain separate questions. | PIR/Interface owns the surface; Relations owns correspondence. A relation mapping one Statement while leaving a Protocol-only public parameter unmapped must permit the mapped check to pass and the exact-surface check to return a meaningful negative. |
| REL-Q2 | Which private Prover inputs and explicitly exported derived outputs form the relation-facing Witness surface, with roles, domains, multiplicities, producers, and lifetimes? Confidential Context, ProtocolValue, nonce/advice, and internal obligation outputs remain distinguishable. | PIR owns the surface; Relations owns correspondence. A private nonwitness forced into the witness image, or an explicitly exported derived witness that cannot be named, fails the cut. |
| REL-Q3 | What execution-issued, capability-backed view identifies the exact qualified run, Statement occurrence/domain/value, and result/object occurrences needed to compare a relation instance with what the verifier actually consumed? | PIR owns the checked execution view; Relations owns instance grounding. A caller-constructed value must refuse, a wrong occurrence/value must disagree, and an exact execution-issued view must pass. |
| REL-Q4 | Which object declarations and occurrences, produced claims, invoked checks, accepted terminals, material bindings, and stable references may a requested correspondence clause read; which are structural versus value-grounded; and is each value bridge an equivalence, embedding, or lossy projection with exact occurrence and loss hooks? | PIR supplies source facts; Relations owns fact, grounding, bridge, and directional-result semantics; Analysis owns quantitative loss. Raw carrier inspection, structural result shape masquerading as behavioral correspondence, or a retained lossy projection with no occurrence/loss hook fails the cut. |
| ANA-Q1 | What visible history, legal move relation, public coins, private inputs/state/randomness, permitted oracle responses, outcomes, and failures parameterize strategy-generated execution? | PIR owns execution structure; Analysis owns adversary and strategy classes. A whole trace correlated with a future challenge that still counts as strategy-generated fails the cut. |
| ANA-Q2 | What single identified declaration determines Statement scope, round/oracle roles, required influence, challenge occurrences, transcript construction, framing, codec/sampler behavior, and the Fresh/FS structural maps read by both admission and theorem applicability? | PIR owns the declaration and checked construction; Analysis owns applicability and transport. Two source declarations that can disagree on a binding or quantitative coordinate fail the cut. |
| ANA-Q3 | Which exact approximation, projection, transform, composition, and retry/failure occurrences may contribute quantitative loss, and which authenticated facts bind their count and parameters? | Source owners expose occurrences; Analysis owns games and loss arithmetic. Any theorem instance needing an authored mirror or hidden count fails the cut. |
| OIR-Q1 | What is the complete role-qualified set of typed inputs, schedule/guards, source events, protected observations/effects, claims, checks, challenges, producer nonproduction obligations, failures, terminals/payloads, imported verifications, source maps, and projection obligations, with stable occurrences and discharge families? | PIR owns the source view; OIR owns exact coverage and its distinction from local validity. Omission, duplication, reordering of a protected effect, phantom provenance, wrong failure/terminal payload, or unsupported discharge must prevent source-relative correctness even when local OIR remains valid. |
| OIR-Q2 | Which external port/proof/Statement facts belong to the exact Interface, which optional Plan facts may change canonical prover OIR, and how is that declared projection basis identified without importing realization-only state or committing irrelevant Plan facts? | PIR owns Interface/Plan meaning; OIR owns its declared read set and identity effect. A realization-only change that rotates OIR, a projection-relevant route change that does not, a required ambient read, or the same fact arriving authoritatively at OIR and Realization fails the cut. K0 freezes no constructor names for the answer. |

Every answer uses the same view law: the source owner defines finite fact
meaning and derives an immutable view from one admitted subject; the consumer
declares the complete read closure and checks adequacy; missing, extra,
wrong-subject, wrong-purpose, and unauthenticated reads have distinct outcomes.
No view becomes a second semantic owner.

## 9. Open scope and representation decisions

These are not invariants and must not be resolved by whichever patch is
shortest. Each row asks one question: **if this selection is wrong, what would
show it, and has that been run?**

A falsifier is a concrete executable observation that comes out differently if
the selection is wrong. It names an input, an object, and an outcome someone
could witness. A restatement of the decision is not a falsifier, and neither is
a task. A falsifier that cannot fail is worse than none, because it reports
success: every row states what result counts as the selection being **wrong**
and why some reachable state of the world produces it. If the only route to a
failing result is a malformed input, the falsifier is vacuous and the row is not
closed by it.

Two tiers:

- **Uncontested.** The rival's rejection is not disputed. The falsifier plus a
  costed rejection of each rival suffices; no rival is built.
- **Contested.** The rejection is genuinely disputed — an external counterpart
  or a downstream consumer needs the rival, or the system already realizes the
  rival's property somewhere, so the inconsistency sits inside one component.
  The rival must actually be built, and both must run on a **shared substrate**;
  without that, inhabitation buys nothing.

Registered rows come first in their existing order, then newly registered rows
grouped by owning domain. Alternatives already recorded are unchanged.

| Decision | Alternatives that remain live | Falsifier | Wrong result | Tier |
|---|---|---|---|---|
| Statement scope | Initial/global only; dynamic/adaptive introduction; both with explicit scopes | A single Sigma protocol fixes its statement up front and cannot exhibit a later introduction at all, so only sequential composition exercises this row; the two named witnesses are a conjunction, not a choice. Take the linked composite (`test/Transforms/pir-link.mlir` under `-pir-link`, projected with the `protocol` selector) and introduce a statement in the consumer segment after the producer's challenge; run seal admission and the transcript prefix laws. Second arm: drive the FRI witness's substituted-statement mutation, published today as NotExercised at `scope:substituted-statement-influence`. | The late binding is refused per segment (`lib/Semantics/SealBattery.cpp:800`) while the composite admits the same statement introduced one segment after the first challenge — "initial/global only" is then a statement about segments and the composite already inhabits dynamic introduction under another name. Reachable: `test/Transforms/pir-segments.mlir` already declares segment decomposition the weak-FS shape, and every existing negative sits inside one segment. Confirms the selection if the second segment must rebind every statement into its own prefix. | Uncontested — no consumer needs adaptive introduction inside a segment; the refusal is shipped and its cost is stated. |
| Meaning of `FiatShamir` | Structurally strong only; generic transcript-derived interpretation plus a separate strong-FS result | Encode a deterministic nonchallenge verifier message into a sealed core (`test/Encoding/grind-schnorr.mlir`, `routed-schnorr.mlir`) and run both readings over the same bytes: seal admission under the structural rule, and the theorem instance `zkc.fs.duplex` through `zkc-derive` against `registry/soundness-signature.json`. Record which layer refuses, and whether the message survives into the transformed interaction under either reading without being named. | Both readings return the same verdict on the same bytes, or the message is admitted under both and no strong-FS result is ever requested — the distinction then lives only in prose, and one of the two readings is unobservable. Reachable: the 21 committed derive refusals are all request-side; no shipped negative separates a structurally strong core from a generically transcript-derived one. Confirms the selection if the strong reading refuses at seal while the generic reading admits and the refusal moves to the theorem instance. | Uncontested — the generic reading requires a separate checked result Analysis does not issue; the rejection is costed, not disputed. |
| Fresh/FS Core relationship | Identical admitted Core; related distinct cores with a checked map; bisimulation or projection relation | Run one admitted Schnorr Core under both interpretations on the shared substrate (`evaluation/r2-p01-schnorr/p01model/semantic.py` with `execution.py:1489ff`), comparing pointwise every TraceEvent, AccessReceipt, ChallengeReceipt, `visible_public_before` at every scheduled occurrence, and the result code of all forty registered negatives. Then apply the construction mutations that must separate them — the absorb-order removals `G1_WIRE_ONLY` and `NONCE_WIRE_ONLY` in `r2model/frigrind.py:159-162`. | Some registered negative changes verdict between the interpretations with no construction mutation applied: one admitted Core does not carry both, and the checked map is doing work admission is claimed to do. The opposite failure is equally decisive: no mutation separates them at all, in which case the construction is unfalsifiable and Core identity is the entire content of the factorization. Reachable: the FS path sets `challenge_actor = Actor.TRANSCRIPT` so no role appears at resolution, while the Fresh path resolves through the environment — the two runs already differ in a recorded field. | Uncontested — the shared-Core hypothesis is favored but explicitly not part of the invariant; the distinct-core and bisimulation rivals are rejected on stated cost. |
| Oracle layer | Finite ordinary messages; first-class oracle publication/query; separate IOR subject linked to Core | Express one real oracle round as finite ordinary messages and ask what an IOR-shaped consumer then cannot state. Over `test/Encoding/sumcheck-fs.mlir` and the FRI family (`lib/Family/FriFamily.cpp`), drive a BCS-style mutation: query positions derived from a challenge, with an opening whose queried index disagrees with the index that challenge selects. Record whether any admitted object refuses it, and whether the ArkLib IOR shape (`docs/ecosystem.md:236`) can be stated over the message encoding with no query event. | The disagreement is caught only by a check operand's own equation, by nothing that knows an oracle was queried — the message encoding then reproduces the external obligation exactly and the first-class oracle buys no observation. From the other side: the encoding cannot state the obligation at all, which refutes the finite-message selection. Reachable: `pir.artifact_verify` exists as a dedicated operation precisely because a constrained check was judged insufficient there, so the repository already contains one case where an event kind was added rather than encoded. | Contested — `docs/ecosystem.md` names ArkLib as the source for IOR and reduction theorem shapes, so an external counterpart needs the rival; build the IOR shape and the message encoding over one sumcheck/FRI core. |
| Repetition and recursion | Finite unrolling; bounded recurrence; verifier-descent/import subject; dynamic schedule | Build one repeated structure two ways over one core — finite unrolling of k rounds, and a bounded recurrence carrying k as a parameter — and compare sealed identity, the endpoint-obligation recomputation, and the transcript prefix laws at k and k+1. Use the one recursive surface that ships: `pir.artifact_verify` (8 uses across `test/`) under the rule that a verified child's assumptions, exports, residuals, and carried obligations discharge or lift into the parent route surface (`docs/spec/endpoints.md` §3.1). | Unrolled and recurrent forms agree on identity and on every obligation at every k the corpus admits — the recurrence then carries a parameter no consumer reads. From the other side: identity moves with k in the unrolled form while the recursive import cannot state the child's carried obligations at all, which refutes finite unrolling. Reachable: nothing in the corpus repeats a round symbolically today; every repetition is authored out, so which outcome occurs is decided by what the lift rule needs. | Contested — recursive imported verification is a shipped surface with its contract reserved, and the parent/child obligation lift is already specified, so the system realizes part of the rival. |
| Regime representation | Allocated opaque roots; content-addressed descriptors; per-domain semantic modules | Rotate a regime and recompute. Take the three regime pins in `tools/remint/sentinels.json` (`protocol-vocabulary`, `construction-profiles`, `soundness-signature`), generate a changed regime with `test/Family/Inputs/vocabulary-drift.py`, and recompute every dependent identity through both implementations (`test/Meta/identity-anchors.test`). Record which identities move, whether the rotation is expressible as a content-addressed descriptor with no allocation, and whether any consumer resolves the pre-rotation root afterwards. | Every dependent identity moves with regime content and no consumer ever resolves a regime by allocated root — the opaque root is then a name for a digest and the descriptor is the selection. The converse is equally decisive: some identity does not move under a semantically material rotation, which breaks regime closure rather than the representation. Reachable: all three pins are already content digests accepted by hand and no runtime rotation path exists, so the failing state is the present one. | Uncontested — a static regime needs an exact change law rather than a fictitious rotation path; per-domain semantic modules are rejected on stated cost. |
| Algorithm representation | Shared strongly normalizing calculus; per-kind DSLs; content-addressed contract-only v0 | Round-trip a real algorithm rather than its citation: encode and decode one value through the shipped codec, step the sampler through a full `(state, conditional inputs) -> (state', value or named failure)` transition including its exhaustion failure, and drive wrong arity, wrong domain, wrong state, and wrong result against each. This is the first execution, not a comparison — today's measurement records both boundaries claimed for algorithm closure as name checks, that a sampler is cited and that its name is supported. | Contract-only references survive every mutation because nothing evaluates, so every negative is a registry lookup failure and the contract-only reading cannot state the failures the shared calculus is being weighed against — the comparison then has no substrate at all. Reachable: it is the measured state; the one real deterministic sampler runs at an unclaimed boundary whose code never fires, and the codec has no decode to round-trip against. | Uncontested — the rivals differ in where semantics live, not in whether an executable is needed; all three stay live until one round-trip runs. |
| Guard representation | Bounded ROBDD; canonical syntax plus certificates; derived ROBDD witness | Build the same guard under two legitimate fixed atom orders and measure build work, not diagram size: construct by `apply` over a unique table with memoisation on operand node pairs (`evaluation/r2-probe-guard-cost/guard_model/guards.py`), across the pairing family at increasing n under interleaved and separated orders, reporting expansions, peak unique-table size, final node count, and the profile bound's refusal. | Work does not vary with the order — a cost model whose cost is independent of the representation says nothing about the representation, and the bounded-diagram selection is unsupported. Reachable and observed: the earlier measurement did exactly that, 65536 predicate evaluations under both orders for diagrams of 18 and 512 nodes, and the figure the witness was cited for was two node counts. | Uncontested — no consumer needs canonical syntax plus certificates; the bounded-diagram rejection now carries a measured price. |
| Schedule identity | Total schedule in Core identity; partial-order semantics with a canonical execution policy; explicit scheduler subject | Two linear extensions of one causal order produce equal CoreIds while an observer distinguishes their executions. Concretely: take one protocol's reduction graph, choose two extensions legal under the same causal order, seal both (`zkc-translate --id`) and execute both, comparing on an observation quotient fixed in advance — verifier-visible protected occurrences, terminal reference and payload, failure reference, acceptance — and compare identity separately. | Two extensions with equal identity differ inside the quotient: the total schedule in identity is choosing one semantics among several no author stated. Or extensions that differ observably always differ in identity and no admitted protocol has two, in which case the total order is a canonicalization wearing an execution rule's clothes and must be restated as such. Reachable: `lib/Encoding/CanonicalEncoder.cpp:467` already does partial-order identity for the reduction graph while the spine commits to a total order. | Contested — one encoder holds both positions, so the inconsistency is inside one component; build both readings on the sealed-artifact substrate. |
| MLIR/canonical split | MLIR semantic carrier; MLIR workbench over neutral bytes; hybrid token-plus-canonical reference | Run identity through the neutral bytes, not through the carrier. Over the fuzz corpus and encoder backstops (`test/Encoding/fuzz-roundtrip.test`, `encoder-backstop.mlir`), require that every identity is reproduced from canonical bytes alone by the twin, that an invalid structure is refused at decode rather than by MLIR verification, and that a rewrite changing MLIR shape without changing canonical bytes leaves identity fixed. | Some identity or refusal is reproducible only through the carrier — an operation name, region shape, or verifier ordering the canonical bytes do not carry — so MLIR is semantic and the workbench reading is false. The converse is equally informative: every judgment survives on bytes alone, and nothing in the identity system may then depend on MLIR. Reachable: parity is held byte for byte against the twin today, so either answer is read off runs that already exist. | Uncontested — the hybrid token reading is rejected on cost; the twin already fixes the boundary. |
| Composition context | Full lineage in challenges; smaller semantic domain ID; provenance excluded | Project a composed protocol and follow the challenge state by SSA value across the segment seam: does the consumer's squeeze consume a state into which the producer's squeeze result was absorbed, or does the segment open a fresh sponge? Read provenance from the sponge, never from the challenge domain string, which link assigns positionally (`left.producer.challenge`, `right.consumer.challenge`) and which would read the same whichever producer had been linked. | The consumer's coin is derived from a state that does not transitively carry producer material — provenance is excluded in fact, and a proof for one composite replays inside another. Or the only carrier of lineage is the domain string, which is a label rather than a binding. Reachable: a link opening the consumer's segment from a fresh sponge is what excluding provenance looks like once implemented; breaking exactly that capture fails this check and no other. | Contested — a replay and interoperability question with a downstream consumer; both readings must run on one linked composite. |
| Plan closure | Rename structural coverage; split open/closed Plan; require total supplier disposition | Run a FRI plan with real holes to a downstream closure: fill each hole through the shipped supplier path (`test/Oir/prover-real-fill.test`, `hole-parameters.test`), leave exactly one hole undisposed, and record which judgment refuses — structural coverage, an open/closed Plan distinction, or a total supplier disposition rule — and at which stage. | The undisposed hole reaches execution and surfaces as an error out of `step()` with no protocol outcome (`lib/Interpreter/Interpreter.cpp:761-766`), so plan closure is not a judgment at all but a fill defect, and renaming structural coverage closes nothing. Reachable: `include/zkc/Interpreter/ExecutionProfile.h:186-192` already states exhaustion is a defect of the fill and never a verdict, so the failing route is the shipped one. | Uncontested — no consumer needs an open Plan as a durable object; each rival's cost is stated. |
| Interface collections | Keyed canonical maps/sets; explicitly observable order | Permute every authored collection whose order the model claims unobservable and compare both the derived identity and the reconstructed member sequences with their per-entry bodies; then take one stable reference into a keyed collection and insert an unrelated member, requiring the reference to still resolve. Run it where identity is derived through owner-local maps (`normalizeReduces`) and where a report is rebuilt from frozen inputs (`r2model/report.py` `build_report`). | Equal identity over unequal reconstructed content: an ordering decided by a producer-scoped coordinate that enters no preimage is nonetheless in the value. Or a stable reference breaks under an unrelated insertion, which refutes the keyed-map reading. Reachable: role and relation enumeration order in `build_report` is a free producer-side choice the canonical sorts are supposed to absorb, and nothing today checks that they do. | Uncontested — this is the identity-field justification mechanism; explicitly observable order is rejectable on cost once one permutation runs. |
| Correspondence regime | Static versioned specification constant; first-class admitted subject | Cold replay plus the authority cycle. Reconstruct every regime a correspondence result depends on from persistent inputs, in a process sharing nothing with the producing run (`python3 tools/remint/remint.py sentinels` over the twelve anchors), then substitute a regime and require refusal. Then trace who admits the regime that admits the correspondence, and check whether any admitting authority is itself named by a regime in that set. | Some correspondence result cannot be reconstructed without a value produced by the run that made it, or the admitting authority is reachable only through a regime the correspondence itself pins — a cycle, which the static-constant reading forbids and which the first-class subject would have to own. Reachable: three of the twelve anchors are regime pins and two are judgment digests, so regime identities and relation identities already sit in one anchor set with no stated ordering between them. | Contested — cold replay is required for every operation needing an admitted regime while the first-class lane has no inhabitant; build the acquired-regime path beside the static constant on the anchor substrate. |
| Result semantics | Structural reference only; directional acceptance relation; full checked equivalence where decidable | Hold one relation/Protocol pair and make the judgments disagree. The relation-to-Protocol law now executes over an admitted relation and an admitted Core, with six drivers including a canonical pair that corresponds while a perturbed witness assignment fails satisfaction. Extend that pair to the judgments the row separates — structural reference, acceptance in the soundness direction and in the completeness direction separately, and full equivalence — and require an admitted pair affirmative under one and negative under another. | No admitted pair separates structural correspondence from acceptance in either direction: the directional relation is then structural binding under a stronger name, which the meaningful-negatives invariant forbids. Measured state: structural correspondence and satisfaction do disagree, while Protocol acceptance, soundness direction, completeness direction, and full equivalence are not exercised at all, so three of the four judgments have no negative today. Reachable: the perturbed-assignment case already produces the disagreement for the one pair that runs. | Contested — Analysis owns the universal theorems that consume the direction, so a downstream consumer needs the rival; both must run over the one relation/Protocol pair. |
| **PIR** — Fixed party cardinality | Three role classes with fixed cardinality, as written; a two-prover interactive-proof Core with a no-communication assumption; verification delegated to or distributed across several verifying parties; PublicEnvironment folded into Verifier so the closed role set is exactly two | Build the fold-into-Verifier rival on one substrate: same occurrences, schedule, and contracts, roles rewritten to (Prover, Verifier), the statement's actor moved off `PUBLIC_ENVIRONMENT` (`p01model/semantic.py:392`), every recipients tuple rewritten, `FreshRealization.resolver` set to VERIFIER. First partition the checks: `admit_core`'s expected role tuple (`:676-687`) and `admit_fresh`'s resolver identity (`:1751`) test the names literally, so parameterize them on the core's declared role set, record them as distinguishing by name only, and exclude them from the verdict. Over everything else run both cores under Fresh and under Fiat-Shamir — public-coin eligibility, Schnorr correspondence, every TraceEvent, AccessReceipt and ChallengeReceipt, `visible_public_before` at every occurrence, all forty registered negatives — counting a difference only where a check branches on role class. | Outside the two name conjuncts nothing changes verdict under either interpretation: the third role class is read by exactly the rules that spell its name, and the cardinality clause is bookkeeping carried by four downstream tables. Reachable: the challenge's declared recipients are (Prover, Verifier), excluding the environment that resolves it (`semantic.py:416-427`, against the rule that successful public challenges become known to all roles); the Fiat-Shamir path sets `challenge_actor = Actor.TRANSCRIPT` so no role appears at resolution; the shipped endpoint vocabulary admits two party programs and no environment program. Confirms the selection if a sampling-failure fact known to a resolver that is neither Prover nor Verifier changes which party may branch on it. | Contested — PIR declares three role classes while PIR-to-OIR projection admits two party programs, so the inconsistency is inside one component; the multi-prover rivals are costed refusals with no consumer. |
| **PIR** — Closed seven-kind effect vocabulary | The seven as written; fewer kinds, with EmitArtifact dropped and RaiseFailure demoted to a check's declared false effect; more kinds, with the four terminal routes the carrier distinguishes as separate kinds; one open event kind whose per-kind equations move into its endpoint-obligation contract | Replace the census with a merge-and-split experiment on the objects that instantiate the kinds. For each unordered pair, form the merged vocabulary (one kind carrying both per-kind equations, discriminated by envelope fields alone) and recompute on every object instantiating either member: per-kind input and protected-observation equations, observation-class completeness, the seven-row endpoint-obligation recomputation, and the admission conjuncts. Separately split ReachTerminal into the four routed sinks the carrier distinguishes and recompute the same five tables. Run over the Schnorr witness (five occurrence kinds), FRI-Grind-1 (six action kinds, Residual a kind rather than a terminal payload), the commitment witness, and the lit corpus mapped through projection rather than counted as raw op names. Port the rules onto FRI-Grind-1, the object whose cut disagrees, so a relaxation is reachable. | Some pairwise merge changes no recomputed row, no classification, and no admission verdict on any inhabited object — two kinds wearing two names — or the terminal split changes a row, meaning one kind carries four the model re-splits downstream. Either falsifies seven as a derived count; the selection is confirmed only if every merge changes at least one row on at least one inhabited object and the split changes none. Reachable: the shipped carrier is a completed counter-partition (bind, slot, chal, check, artifact_verify, end, plus four routed sinks, with no artifact-emitting operation), and the census over `test/` is mechanical — 8 artifact verifications, 117 residuals, 68 discharges, 7 exports, 3 assumes, 0 emits. EmitArtifact's uninhabitance is reported separately as a fact about that kind's readership, not as evidence about the count. | Contested — four independently written cuts of one effect space run at once: the dialect, the Schnorr witness, FRI-Grind-1, and the kernel's six-item protected-effect list. Shared substrate is the lit corpus plus the finite witnesses. |
| **PIR** — Observation is not knowledge transfer | Publication derived from the protected-observation class, so a public or terminal payload publishes and a wire payload transfers to declared endpoints; publication authored per occurrence as an explicit recipient set and checked against the class, which the executable witness already does; the current split, kind-derived closure with two hand-added exception rules and one blanket denial | Build the rival as the direct negation: transcript-class observation transfers to every role, check, artifact, claim, and failure observation to their declared observers, with the two hand-added publication rules deleted as now derived. Compute `KnowsAt(PublicEnvironment, commitment)` at the challenge boundary under both closures — the commitment's recipients are (Verifier,) at `semantic.py:404-414` while the Fiat-Shamir construction absorbs it — then recompute pointwise the terminal knowledge obligation, the role-knowledge and path-availability admission conjunct, all forty negatives including the recipients mutation at `tests/test_p01.py:324`, and `G1_WIRE_ONLY`, `NONCE_WIRE_ONLY`, `VERIFIER_PRIVATE_DEPENDENCY`. Add the check the text cannot make: under each closure, is the derivation well-formed when `derive_fs_challenge` reads a commitment its resolver is forbidden to know? Count a divergence only where a consumer branches. | The closures differ on knowledge for the resolver — they will — and nothing reads the difference: same admission verdict, identical terminal obligation, same forty result codes, well-formed derivation under both. The clause plus its two exception rules then buy a distinction nothing branches on. Reachable: the witness's only visibility consumer computes from recipients plus actor and is untouched by either closure, so "nothing reads it" is the ordinary outcome. Confirms the selection if availability refuses a role that knows a payload before its declared receive, or the recipients mutation changes code between closures, or only the selected closure makes the derivation ill-formed. | Contested — the authored-recipients rival already runs in the same witness and already disagrees with the publication rule; no executed negative varies knowledge today, only observation channel and visibility, which is the trap this ledger has already been caught by once. |
| **PIR** — Terminal result alphabet | Three-valued Accept/Reject/Abort with a total class-to-result map; binary Accept/Reject with Abort folded into Reject and the class reported separately; class-indexed verdict with no collapse layer, which the shipped differential already runs; three values retained but the class-to-result map deleted as a separate free choice | Enumerate every consumer of a terminal value and record the finest distinction each draws. Three sets: the model's own rules; every reader of the interpreter's verdict across `lib/`, `tools/`, `test/Oir/` and the twin's return dict; every reader of the probe's disposition outside the terminal law. Bucket each read as Accept-ness only, branching on class or actor or provenance, branching on Reject-versus-Abort with the class out of scope, or merely carrying, printing, or golden-comparing. Then run the two cross-pairings the alphabet permits and the probe has never run — Abort with a verifier actor and deterministic provenance, Reject with a prover actor — and record which predicate refuses. On the shipped side, replace the five class-indexed verdict strings with collapsed values and record which breakages are decisions and which are expected-output files. | The third bucket is empty: every discriminating consumer either asks whether the outcome is Accept or reads the class, actor, or provenance it already holds, and the cross-pairings are refused by hard-coded label-actor pairing rather than by any rule reading the value alone. Abort then carries no bit not recoverable from the class. Reachable: it is the present state — the only cross-implementation acceptance surface, held byte for byte by lit, has no Abort in its alphabet at all; the class is the verdict and the map is never applied. A lit fixture breaking because verdict strings were renamed counts against the selection, not for it, and the probe has no Accept, so the accepting side can only be settled on the shipped boundary. | Contested — the class-indexed rival runs on the one conformance boundary zkc owns, and the binary reading is already realized by the acceptance rule; substrate exists on both sides with no new machinery. |
| **PIR** — Failure continuation (non-absorbing failure) | Non-absorbing failure with a continuing effect and both the status value and the total failure predicate; failure as an absorbing state, which every shipped verdict path realizes; no failure construct at all, with the check's ordinary Boolean routed by the guard decision; keep continuation but delete the status value and retain only the total predicate | Two arms. (A) The base Core already runs an honest continuing check with no failure declaration — its false value is recovered by two routes and its absence is pinned as Core well-formedness. Drive it false, run the full pipeline, then run the continue-after-failed-grinding variant whose check does carry a continuing declaration. For each continuing path, record every read of the failure declaration after the check resolves false and compare the canonical records field by field: trace events, per-role protected observations, consumed counts, drift digest. (B) Partiality is not a runtime question and must not be run as one — the status token is a constant function of the failure reference — so search the availability and guard-reading rules for a program pair the model admits under the status value and refuses under the total predicate, recording for each refusal whether it comes from the partial constructor or from the ordinary availability rule applied to the total Boolean. | (A) The only post-resolution read is the executor's test of the failure effect deciding not to terminate, with every downstream consumer reading the ordinary Boolean: the continuing effect then means "do not terminate", which omitting the declaration already expresses and which the base Core proves is not merely legal but pinned. (B) Every refusal is produced by availability on the total predicate, so the status value is a duplicate encoding and the fourth alternative is the selection. Reachable: both facts are stated in the owning section rather than reached by contrivance. Vacuity guard: the applicability report fires on a bare label comparison, so its firing is not the observation, and a longer trace is not by itself a difference. | Contested — absorbing failure is what the system runs everywhere a verdict is reached, with first-verdict return pinned by lit, while the document commits to non-absorbing; the only executed continuation in the tree is filed as a security mutation, not as an inhabitant. |
| **PIR** — Prover nonproduction plane | Second top-level execution outcome alongside Terminal; a verifier-visible failure inside the existing failure sum, reaching an Abort terminal; a terminal in its own right, which the probe does; outside Core semantics entirely as a defect of the fill, which the shipped interpreter does; PIR reports only the cause and Analysis owns which plane it lands in | Take the one phenomenon with three inhabited plane assignments — grinding nonce exhaustion — at its three sites: the probe's Abort with a prover actor and prover-strategy provenance, the shipped error out of `step()` with its contract that exhaustion is a defect of the fill, and the document's nonproduction outcome. Enumerate every consumer at each site and record whether its behavior differs between prover nonproduction, an ordinary Reject terminal, and a verifier-visible failure terminal at the same event, and whether that consumer lives in PIR or in Analysis. One executable discrimination: run the exhausted-window witness, then delete the abort law's actor and provenance predicates and observe whether the remaining law still separates the exhaustion record from every reject record. | No consumer distinguishes nonproduction from an ordinary terminal beyond asking whether the outcome is Accept — the second plane then duplicates the terminal plane under a stronger name. Or the only consumer that distinguishes it lives in Analysis, where honest-prover nonproduction is a completeness loss with no soundness consequence, in which case PIR reports the cause and Analysis owns the plane. Reachable: nonproduction is by construction the absence of a message, so the verifier has nothing to see, and the shipped path already turns exhaustion into an error with no protocol outcome. Guards: "the fill errors out so the verifier never runs" is evidence for the fourth alternative, not for the selection; a differing diagnostic identifier is not a differing plane; a consumer that merely carries the failure reference distinguishes nothing. | Contested — three rival plane assignments for one phenomenon already run and disagree with the document and with each other; the shared substrate is FRI grinding, reachable from both the probe and the shipped interpreter. |
| **PIR** — Linear-resource discipline for claims | Linear, exactly one consumer reached before every terminal reachable from the production path; affine with an explicit discard obligation at the drop site; relevant, at least one consumer with duplication permitted; unrestricted claims plus a separate terminal-closure judgment; an explicit two-sorted linear/persistent split, which the model half-states one sentence later | Extend the commitment Core executor — which already carries typed actions, a schedule, a one-consumer rule and a producer-before-consumer rule — with exactly three additions: a per-event activation guard, a live-claim set updated as the schedule executes, and a terminal action that commits and stops. Transcribe the two readings as predicates over one protocol object: COUNT, the shipped use-count verbatim, and PATH, the consumer occurring before every terminal reachable from the production path. Enumerate the guarded protocols the executor admits — the escape pair (consumer under a guard, terminal under its negation), its unguarded twin, an affine variant dropped on one branch with a named discard, and the variant where a claim covered by a verification is consumed both by its routing sink and by the verification. Record COUNT's verdict, PATH's verdict, and whether the executed terminal state holds a live claim. | COUNT and PATH agree on every protocol in the enumerated space and no execution reaches a terminal holding a live claim: the second half of the rule then constrains nothing the use count does not, and terminal closure belongs to the separate judgment the fourth alternative names. Or the verification variant executes with COUNT accepting because its second consumer is carried as an untyped route string, in which case "exactly one consumer" counts typed uses, a second real consumer already exists in shipped code, and the discipline in force is the two-sorted split. Reachable: both shipped checks are pure occurrence counts with no notion of guard, path, or terminal reachability, and the shipped spine is a linear chain with no guard, so no existing witness has ever separated the readings. | Contested — persistent claims with finitely many consumers live in the same claim sort two lines below the selection, and artifact verification already routes the claim it covers as an untyped string because a second use would break linearity. |
| **PIR** — Claim plane derivation | Derived deterministic saturation keyed on the least canonical reduction reference, outside the spine; reductions as scheduled spine events like every other effect, authored and identity-bearing; partial-order plane with an authored linearization only where two enabled reductions are observably distinguishable; derived plane with firing order excluded from identity entirely and no canonical key | Implement the enabling predicate and the saturation loop directly over the model's own claim and reduction shapes on the r2 substrate, seeded with two families: the reduction graph of `test/Encoding/normalization.mlir` protocol "a" transcribed into those shapes, and a guarded family in which one reduction's output-claim parameter exists on one branch only. For each protocol, enumerate every firing order legal at each closure step, not only the least-key one, and compare orders on an observation quotient fixed in advance: verifier-visible protected occurrences, terminal reference and payload, failure reference, acceptance, and the multiset — not the sequence — of claim productions and consumptions. Exclude the production and consumption sequence, and say why in the harness: it records the firing order by definition. | Two legal orders of one admitted protocol differ inside the quotient: the firing rule is choosing one semantics among several no author stated and admission has no rule to refuse, so the plane is scheduled in fact and reductions belong in the spine. The converse is the likelier finding and equally decisive: no protocol in the admitted space can present two simultaneously enabled reductions differing inside the quotient, so the canonical key never breaks an observable tie and must be restated as an identity namespace rather than as the firing law. Reachable: enabling is not monotone across a closure step — firing removes liveness, and under activation guards a parameter can exist on one branch only — and the one-consumer law that would forbid competition is enforced as a use count. Corroboration: the encoder's ready key ends with an authored field that can never fire. | Contested — reductions are authored as ordinary operations in program order beside the spine and only the encoder erases that order, and the same encoder refuses identical source keys outright while breaking identical reduce keys by authored order. |
| **PIR** — Prover binding-failure precedence | Fixed kind-rank precedence over the five causes; earliest-by-boundary-rank then record position, with the missing-output cause as residual since an absence has no position; report the full set of live causes and let the consumer rank them; leave the cause implementation-chosen and keep only the nonproduction outcome | Build one inhabitant of the prover-obligation sections on the substrate that already separates a verifier-visible failure from a non-modeled residual: one protocol, two prover-acted events, the first obligation carrying at least two output ordinals and two private randomness members. Enumerate the finite space of well-formed trace-and-replay pairs over it — existing references, in-domain canonical values, nondecreasing boundary ranks, same-boundary records in supplied order, randomness in schedule then basis order, legal failure tags only — and compute two selectors side by side over the same trace: the fixed rank applied over all live causes, and the staged scan that walks the schedule and takes the first cause the staging reaches. Record, for both, the selected cause reference, the consumed record count, and the partial state. | The two selectors agree on every well-formed pair: the fixed rank has no content beyond what the trace grammar and the attempt staging already fix, it restates its own derivation, and the section should state the scan and delete the ranking — the same defect this repository deleted elsewhere in its own words. The second wrong result reaches the same conclusion from the other side: separation exists but every separating witness needs a cause outside its legal position, so the separation is a property of malformed input. Reachable: a multi-cause trace is well-formed — a repeated ordinal and a failed private sample are declared causes, not malformations — and boundary order and kind order genuinely disagree, since the missing-output cause has no record position and a sampling failure before the first event precedes every pre-action record while ranking below the early-output cause. | Uncontested — no external counterpart models prover nonproduction and the only consumer is the byte-for-byte differential, which needs determinism rather than this determinism; the real exposure here is vacuity, not contest. |
| **Analysis** — Fiat-Shamir reasoning cut into three contracts | Three separately requestable results: a PIR-owned construction check, an Analysis-owned theorem instance, an Analysis-owned property transport; two results, folding applicability into transport; one fused compilation result checking side conditions, instantiating the theorem, and transporting the bound in a single rule application | Seal one artifact, hold its bytes fixed, and perturb one layer at a time. Control: the sumcheck fixture under the soundness signature with its committed request derives at exit 0. (a) Rebuild with the extension-field codec narrowed so the fold challenge cannot frame its sample space — construction facts withdrawn, request unchanged. (b) Move only the target's model coordinate to a quantum random oracle, over identical bytes and plan. (c) Move only the quantification coordinate to adaptive, again over identical bytes. Ask whether these are three states of one subject: do (b) and (c) still carry the sealed construction facts that (a) removes, checkable by deriving the control over those same bytes, and does (c) still admit the theorem instance (b) does not. Record three independent bits, then separately record how many outcome classes the shipped exits and diagnostics expose over the three. | The bits do not separate — a perturbation that moves only a target coordinate also withdraws the construction facts, so no reachable request distinguishes an inadmissible construction from an admitted construction with no theorem instance, and the fused rule loses nothing. Measured: the bits do separate, and the report collapses them. (b) and (c) return a byte-identical premise mismatch at exit 1 and (a) returns an invalid rule payload at exit 1, so a theorem-correspondence coordinate the properties document lists by name reaches the consumer as a malformed payload while an absent theorem reaches it as a negative property. The three-way cut has content the shipped realization does not carry, and the exit vocabulary that would carry it does not exist: three codes, no unsupported class. | Contested — the rejected rival is the shipped realization: one rule whose machine conditions check the construction, whose conclusion index pins model and quantification, and whose body carries the quantitative transport. This is also the first decision registered in Analysis, so the domain arrives unexamined rather than disciplined. |
| **Analysis** — Replay bundles exist only under three named triggers | Three triggers only — named independent consumer, expensive reconstruction, real cross-process trust separation — with recomputation the default and convenience disqualified; persist every checked analysis result by default; a fourth trigger for a decisive regime the subject bytes do not carry | Seal one artifact and hold its bytes fixed. Derive its committed request under one signature and keep the witness. Take a legitimate second analysis, not a corruption — the same signature with one rule's move budget doubled — and run two routes over the same artifact, giving each route's consumer only the durable bytes that route hands it, never console output. Route one recomputes with no witness and leaves a judgment digest. Route two re-checks the first witness under the second signature. Then ask the two questions the trigger rule turns on: can a later party holding only route one's leavings determine which analysis regime produced them, and does the persisted record earn the trigger it must claim — time both routes over enough iterations to resolve the difference. | Persistence demonstrably performs a function that is not one of the three triggers while the trigger it would have to claim is measurably false. Both halves were observed: recompute exits 0 with a different judgment digest over the unchanged artifact and no diagnostic of any kind, so the regime is silently substituted, while the witness route names both signature digests and refuses at exit 1 — the demonstrated function is regime pinning. And checking re-runs the entire derivation before comparing, so twenty runs of each cost the same, about 27 ms per run; persistence saves no reconstruction whatever. Reachable: two honest analyses of one protocol under different theorem catalogues is already a committed expectation, not a malformed input. Confirms the selection if the judgment digest's own preimage bound the signature, or if checking read the recorded conclusion instead of re-deriving. | Contested — persist-without-a-trigger is what the tree already does, since a witness is written on request for every run including consumers that could recompute; the one trigger with an independent executable inhabitant is trust separation, and it is inhabited in the backend lane rather than in the lane the rule governs. |
| **Analysis** — Dual-lane identity with forward taint | Single portable content-identity lane, any nonserializable dependency refused at formation; dual lane with forward-only preimage taint; dual lane with backward closure, where a local support taints the antecedents that consumed it; portable-with-declared-hole, the local child replaced by a typed content-addressed absence marker | Two arms, both on values whose declared preimage names only public identifiers. (A) Permute a producer-side enumeration order no preimage names — the order in which the four execution roles and the relation names are constructed in the report builder — rebuild, and compare both the report identity and the reconstructed member sequences, including per-entry evidence bodies. (B) Recompute every durable identity the tree emits — the twelve sentinel anchors, the full semantic-identity graph under `--check`, the capture and replay transcript digests — from a second process with working directory, absolute artifact paths, temp directory, environment, hash seed, and directory enumeration order all changed; then re-run the one foreign-owner live result, whose acceptance is produced by an implementation zkc does not own, from the checked-in fixture alone. Record for each durable identity whether the identity reproduces and whether the content it names reproduces with it. | (A) The identity is unchanged while a reconstructed member sequence changes — equal portable identity over unequal content, so an ordering decided by a producer-scoped coordinate that enters no preimage is nonetheless in the value, and the forward-only rule has classified as fully portable something whose identity does not determine its content. (B) Some durable identity fails to reproduce, or reproduces while its content differs, under a change touching nothing in its declared preimage; or the foreign-owner graded result is emitted as a public durable result and is not reproducible from persistent inputs. Reachable: the local canonical order is valid only inside one owner instance while taint propagates only along preimage edges, so an ordering with owner-local inputs and public outputs is untainted by construction — and the encoder already positions identity children through owner-local value maps with only the derived positions entering the preimage. | Contested — the portable-only rival is fully realized (twelve anchors recomputed from persistent inputs, a cold-replayed capture with mutations refused) and zero shipped identities inhabit the owner-local lane; the ledger's own cold-replay requirement carves out no such class. |
| **Project** — No universal transition algebra, artifact, or transition identity | Domain-owned typed transition contracts under the shared project invariants, with no shared identity and no shared envelope; one universal transition algebra carrying a transition identity, wire envelope, checker registry, fact root, and composition law; a machine-readable projection of the shared schema used only to lint catalog completeness, with identities still per-domain | Hold one sealed artifact fixed and ask whether the cross-domain reference's ordinal coordinate is determined by identities each domain already owns, or only by the run that produced it. Seal the multi-application KZG fixture and run the PIR compiler provider over those same bytes twice under the same three pinned configuration references, varying only a legitimate selection input the provider accepts — the application and plan limits, or the group selection — so both runs are honest compilations of one artifact. Record the family-and-ordinal pairs each run assigns, as the provider already prints them. Then fabricate a preservation obligation naming a nonexistent family at index 9999 and ask whether the recorded pair set suffices to refuse it, and whether that set is a function of the sealed artifact and configuration references alone. Keep the neighbouring anchor-grounding edit, refused at exit 1, as the control that fixes everything except the coordinate at issue. The question is resolvability of the reference, never checking of the claimed property. | Two honest compilations of one artifact under one configuration assign different pairs to the same transform: the ordinal is a coordinate of the run rather than of the artifact, and refusing the fabrication would require an identity whose preimage covers the applied transition — which the tree does not have, since the checked transform trace carries no digest and never crosses the library boundary. What must not be counted as the result: the bare observation that the fabricated tuple re-derives at exit 0 while the anchor edit refuses at exit 1. Both edits were run today and produce exactly that, and it is produced by the rival world and the selected world alike. Reachable: the tuple is duplicated by hand across the domain boundary and the checker has the artifact and the signature, not the trace that produced the artifact, so every honest producer emits a reference in the state the falsifier fabricates. | Contested — the system needs the rival's property at the one point two transition families meet and already realizes it for the adjacent coordinate inside the same document; the twelve anchors show per-domain identities genuinely differing in kind, so the row is not rigged toward its conclusion. |
| **Project** — Stage 4A whole-architecture target | Enlarge the current closed Soundness kernel and enumerated Compiler; one universal typed proof and dependency graph as the checking and replay substrate with owner-defined semantics retained; exact external proof obligations and adapters as the primary Analysis mechanism; checked certificates plus relational synthesis and symbolic finite-domain optimization; the selected family-indexed Analysis under a small common lifecycle plus five authority-separated Compiler planes | Measure the decision plane at the granularity the target's own candidate-identity section owns, and let the shipped transform families decide whether a contest exists. For every request the shipped suites admit, enumerate the candidate domain as distinct transform-problem plus transition-case, collapsing plans that differ only by schema alternative, producer, recipe, or proposal ordinal, and record two numbers per request: distinct candidate identities and distinct score vectors over them. For any request whose domain has two or more members carrying equal score vectors, permute the production plane — swap the transform-domain provider's enumeration order — and record which candidate identity the selector returns, resolved back through the plan domain rather than compared as ordinals. | Either branch. (a) An admitted request has two or more distinct candidates on equal scores and the returned candidate differs under the permutation: a production-plane value the target excludes from candidate identity then solely determines a decision-plane answer, at the granularity the target legislates rather than the one it disclaims. (b) Every admitted request yields a single-member candidate domain: the closed domain, the bounded-best rule, and the complete-frontier rule are unexercised by every transform this repository can execute, and the decision plane has never once been asked to choose. Confirmed only by the third outcome — two or more candidates, objectives that separate them, and a candidate invariant under permutation. Reachable: both alternatives at the shipped two-plan tie are legitimately declared, both pass validation, both score 16, and the fixture already asserts the ordinal fallthrough as expected behavior; which branch occurs is decided by shipped code rather than by the experimenter. | Contested — the rival candidate's central choice is the running system: one security-index payload carries three tracks through one result schema, one closed bound algebra, and one rule table, which is exactly the universal-payload property the selected target declines to adopt. |
| **Project** — Two-domain comparison model | One semantic candidate domain with qualification carried as a typed constraint that filters candidates, which is how the compiler works today; the selected separate comparison-alternative domain derived by a total qualification resolution; one candidate domain with qualification-indexed score vectors, so basis trust is an objective coordinate rather than a second domain | Hold the transform plan fixed, vary only the qualification alternative, and write no new resolution rule. Re-run the two-alternative fixture with the two schemas given different soundness resource substitutions instead of two identity substitutions, so the accepted derivation results genuinely differ while plan, subject, target, and declared objective stay identical. Record three things: the objective values scoring assigns each alternative; every field of the compiler result and the decision verdict that differs between a run selecting one and a run selecting the other; and whether the decision checker accepts a record produced under the other alternative. Then read the scoring path and the objective kinds and record whether any declarable objective is a function of the qualification alternative at all. | Objective values equal and both records identical, so no authorized consumer of the decision reads which qualification was accepted: a second domain whose members are indistinguishable in every authorized observation is an identity that changes nothing, and the accepted cost of two closed domains is paid on a system where the choice provably affects nothing. Sharpened sub-outcome: if the only way the two alternatives are ever distinguished is that a constraint renders one ineligible, qualification is behaving as a candidate filter and the single-domain rival wins, since the two-domain model requires both alternatives to survive and be compared. Reachable: scoring computes every objective from the final artifact's observation and codec widths and never touches the derivations, and the objective kinds admit static proof bytes and unsupported. | Contested — the rival is the shipped implementation by the document's own account, and one request already drives domain formation, validation, scoring, and selection, so both readings run from the identical fixture varying only the dimension the row is about. |
| **Project** — Portable decision carries no target carrier | The portable decision embeds the canonical target carrier and reconstruction manifest, so decision replay permission delivers the target; the selected separate handoff bundle authorized for an exact downstream consumer and typed purpose; the decision carries only a content digest of the selected target, so identity travels with the decision while the carrier is fetched under a separate permission | Conduct the reconstruction on serialized bytes only, in a process holding no handle from the compiling process. Compile the same-point KZG fixture and write down exactly what a portable decision is defined to carry — one optional ordinal, plus whatever "transitively identifies its selected candidate" resolves to on that record. Then, in a fresh process given only those bytes plus the canonical predecessor carrier bytes the snapshot produces, recover the selected plan's canonical application, realize, seal, and compare the sealed identity against the selected target's. Record whether the fresh process can name which predecessor to load from the decision record alone, and whether it can complete realize, the seal judgment, snapshot, and admission without any authority, capability, or environment object it was not handed. | The fresh process emits a byte-identical target: the handoff bundle is then a permission boundary with no representational content behind it, its exclusive content is empty, and the accepted price falls only on cooperating consumers while an uncooperative one re-derives the target from material replay already delivers — a shim with no trust boundary behind it. Equally decisive from the other side: the fresh process cannot even name the predecessor, because the decision is one ordinal into a domain it cannot reconstruct, so the portable decision does not transitively identify its selected candidate and the premise the split rests on is false. Confirmed only if it names the predecessor and still cannot produce the target. Reachable: the check path is on the required transform-checking route, replays realization, and compares sealed identities, and the request carries the predecessor carrier as a field. | Uncontested — the downstream stage is unactivated and no component realizes a carrying decision. A near miss on the line: reread it if the falsifier fires, since the replay-reconstruction path realizes the effect the rival would supply. |
| **Compiler** — Seven-class qualified-outcome envelope | Seven fixed classes; the eleven-class vocabulary already running in the witnesses, which adds mismatch, missing dependency, resource exceeded, and not exercised; a minimal three-class outer envelope with domain-owned refinements, matching the tools' shipped exits; an open domain-keyed outcome registry with a shared non-interchangeability law | First make the envelope's owner write down the total map from the eleven running outcome classes to the seven; without a declared map there is nothing to test. Then, over the corpus unchanged, compute per witness the set of outcome-and-boundary pairs — dropping the code, which is unique per site and therefore hides any collapse — under both labelings, and report every boundary at which two distinct running outcomes land in one envelope class. Run this on measured output rather than on source: instrument the coverage tool to emit its reached triples, and confirm both members of each collision are actually driven by some mutation. Second arm, the consumer side: find every consumer that branches on outcome class. Today two do. | The outcome-and-boundary cardinality strictly falls: some boundary loses a distinction the running corpus makes, so at that boundary the seven classes cannot state what the checks that ran did state, and any invariant filed closed there is closed by the vocabulary rather than by the checks. On the corpus as it stands the drafted map gives 339 pairs before and 336 after, collapsing at three boundaries, subject to the runtime confirmation. Two-sided: a declared map holding at 339 with every collision unreached at runtime is the envelope's answer and closes the row. The second arm has its own failing result — if no consumer branches beyond checker failure and the affirmative split, seven versus eleven is unobservable to every running consumer. | Contested — three outcome vocabularies run at once and disagree on how many classes exist: eleven in the witnesses, seven in the durable corpus, three at the tool boundary; and the outcome-separation invariant independently requires missing dependency and resource exhaustion to stay distinct, neither of which the seven carry. |
| **Compiler** — Path-sensitive candidate identity | Path-sensitive by default with an opt-in consumer-relative quotient; path-insensitive by default with opt-in refinement where a consumer names the observation that separates the paths; semantic path never in candidate identity, carried only in the qualification, lineage, and replay records | Gate first, and treat it as a gate: the two routes must provably reach one target — seal both and diff the identity; if the identities differ the run is void, because that is a different protocol rather than a different route to one. Two pairs pass. (a) The relabel pair, which reaches one identity through two authored transformer orders and two label sets. (b) One batched opening reached two ways over the same claim graph: authored directly as a reduce against the batch contract, versus produced by the batch-open pass, both sealed under the same vocabulary and construction-profile registry. With equal identities established, compare byte for byte the canonical output and every value the target declares a decision policy reads: constraint results, objective values, comparison coordinates, replay reconstruction material. Assert nothing about candidate identity; enumerate the declared reads and check each. | The routes agree on the identity and on every declared read: no declared observation separates two members the default separates, so a closed decision's completeness, complete-frontier, and no-eligible claims are stated over a domain whose member count tracks producer routes — which candidate identity already excludes for producer, recipe, and search order — and a caller receiving only the sealed target and its identity cannot tell the winners apart, so optimality over the finer domain does not transfer. Reachable: the encoder canonicalizes the reduction graph by a semantic content key and the shipped relabel pair already reaches one identity from two authorings. Two-sided: any declared read that differs — a segment, a residual route, a discharge, an objective value — earns the separation and closes the row for the selection. | Contested — the target ships a candidate quotient and domain policy whose whole job is to merge what the default separated, so it holds both positions at once; a downstream caller receiving the selected target needs the rival, and the shipped precedent puts route in the derivation rather than in the subject. |
| **Compiler** — Open report as a successful side exit | A distinct successful outcome with its own strictly weaker capability; the report folded into the qualified non-results as a cannot-answer carrying retained partial facts; no report path, every operation reaching a closed decision or an outer non-result; a report whose claimed accounting scope, audit ledger, and blocker set sit inside its own identity digest rather than beside it | Hold the subject fixed and vary only the discharge verdict. Seal one source twice — once as authored, once with the analysis-only policy on the protocol operation — retarget the request to the second identity, and run the derivation on both without inverting the expected status. Record at field level rather than as a raw diff: each exit status; the witness field sets and the identity field set; which fields differ and whether each is explained by the substituted artifact identity alone; whether the conclusion is unchanged; and whether the judgment digest, recomputed from the witness's own preimage, is a function of the verdict or of the identity. Add the schema arm — the encoder's fixed object and the parser's closed extras and identity sets — and the consumer arm, any caller that branches on the difference. Keep the two-different-artifact pair as the control that shows the pattern carries the input, not the verdict. | Both runs exit 0; the witness field set is identical and carries no typed discharge, policy, accounting-scope, or blocker field; the conclusion is byte-identical across the flip; every differing field is explained by the substituted identity, and the judgment digest tracks the identity rather than the verdict. The durable artifact of an open report is then shaped exactly like a closed one, separated only by a prose line on stdout, and the closed identity schema means no consumer can read the difference. That is what the run produced today. Two-sided: a discharge, policy, or accounting field in the witness, the judgment entering the digest preimage, or a consumer that refuses or flags the open witness flips it — and the tree already does exactly that for an assumed premise, whose marker travels in the witness. | Uncontested — nobody disputes that an honestly scoped partial answer is a success rather than a failure, and each rival is rejectable on stated cost. The live question is distinguishability to the consumers that read it, which the falsifier carries alone. |

### What is already delivered

Five falsifiers ran today, and three of them fired.

**Guard representation.** The diagram is now built the way an implementation
builds one, by `apply` over a unique table with memoisation on operand node
pairs. Work then tracks structure: the interleaved order costs exactly n²
expansions and the separated order more than doubles at every step, so the two
orders separate in work as well as in size and the gap widens with scale. The
previous measurement recursed over every assignment and cost 65536 predicate
evaluations under both orders for diagrams of 18 and 512 nodes — diagram size
wearing the name of build work. Two further defects surfaced: the declared node
bound never bound anything (512 against a 512-node diagram compared with a
strict inequality; now 256, shown refusing the adversarial case), and only one
of the two admitted atom kinds was exercised.

**Composition context.** A composed protocol had never been projected. With the
projection selector added, the challenge state is now followed by value across
the segment seam: the producer's squeeze result is absorbed into the state the
consumer's own values extend, and the consumer's squeeze consumes exactly that
state. Nothing resets at the boundary. The answer is in the sponge, not in the
challenge domain string, whose prefixes are positional and would read the same
whichever producer had been linked. Excluding provenance is a reachable
implementation — a link opening the consumer's segment from a fresh sponge —
and breaking exactly that capture fails this check and no other.

**Fiat-Shamir three-way cut.** The three subject bits separate: the same bytes
that refuse under a moved model coordinate or a moved quantification coordinate
derive at exit 0 under the control, so construction admission is independent of
the requested index. The report collapses what the subject separates — the two
index perturbations return a byte-identical premise mismatch, the codec
narrowing returns an invalid rule payload, all at exit 1 — and the outcome
vocabulary that would carry the distinction does not exist.

**Replay bundle triggers.** Recomputation under a changed signature exits 0 and
returns a different judgment digest over the unchanged artifact with no
diagnostic, so the analysis regime is silently substituted; the witness route
names both signature digests and refuses. The persisted record's demonstrated
function is regime pinning, which is not among the three triggers, and the
trigger it would have to claim is false by measurement: checking re-runs the
entire derivation before comparing, so both routes cost about 27 ms per run.

**Open report.** The two runs differ only by the substituted artifact identity.
The witness carries no discharge, policy, accounting, or blocker field, the
conclusion is byte-identical, and the judgment digest tracks the identity rather
than the verdict.

Partially delivered: the outcome-envelope collapse count (339 pairs before, 336
after, three collapsing boundaries) awaits the runtime confirmation that both
members of each collision are actually driven. The relation-to-Protocol law now
executes with six drivers, including a canonical pair that corresponds while a
perturbed witness assignment fails satisfaction, which delivers the first arm of
result semantics; Protocol acceptance, soundness direction, completeness
direction, and full equivalence remain undriven. Statement scope's substitution
arm is published as not exercised. Algorithm representation has a measured
negative rather than a run: both boundaries claimed for algorithm closure are
name checks, nothing encodes a value or steps a sampler, so no round-trip
exists yet.

For the transition-algebra row, both document edits were executed and the
fabricated obligation re-derives at exit 0 while the anchor-grounding edit
refuses at exit 1 — recorded here as **not** the result, since the selected
world and the rival world both produce it. The two-compilation ordinal run is
outstanding.

Every other row is unrun. Measured invariant closure across the witnesses now
stands at 3 closed, 15 partial, 3 open of 21 tracked; the number fell when
boundary names stopped counting as evidence, which is the same failure this
section is now built to catch.

## 10. R1 gate

R1 is internally complete when every adjudicated finding maps to this ledger,
a rebuttal, or an explicit current-system finding, and the independent
validation brief has been issued. The first cold pass failed the gate and
required material corrections to this ledger; its outcome is recorded in
[validation-outcome.md](validation-outcome.md). No item here authorizes durable
rewrites. R2 begins only after a fresh follow-up falsifier validates the amended
record or its further material corrections are incorporated and rechecked.

## 11. Why the selection standard changed

X-05 was a completeness test and became a falsifiability test on 2026-08-24.
Executable inhabitance filtered nothing: read literally it asks for one
inhabitant, which all fifteen registered rows already have. The stronger
unwritten reading — build every alternative and compare — failed against the
repository in both directions. It is not sufficient: composition context has
all three alternatives inhabited by running code and remains incomparable,
because no executable object in the repository carries a composition context at
the moment a challenge is derived, so nothing varies the dimension the row is
about. It is not necessary: four alternatives are unrepresentable because a
shipped refusal makes them so — a late statement is refused by `zkc-E214`,
sampling is bounded and loop-free by design — and inhabiting them means deleting
a guarantee to measure the hole, where the refusing code is already the
argument. Meanwhile the failure that actually occurred, four times in one day,
was a real artifact sitting adjacent to a claimed property and counted as it: an
invariant filed closed on a boundary testing something else; a guard witness
reporting diagram size as build work, where two variable orders both cost 65536
predicate evaluations while the diagrams differed 28-fold; a witness-surface
invariant closed by an identity-equality check; a lane survey grading the topic
instead of the discriminating property. Building more alternatives produces more
of these, not fewer. The question each row now answers is: if this selection is
wrong, what would show it, and has that been run.
