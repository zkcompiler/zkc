# Recursive-Composition Closure and Absorption Record

> **Kind:** Temporary package-closure record
> **State:** Completed
> **Authority:** None. The durable pages named below own the retained target
> semantics.

## Decision closure

The package selected the owner-separated model. One protocol step remains a
PIR execution; exact public/private recurrence and complete recursion-facing
binding coverage remain Relations checks; family and theorem meaning remain
Analysis; and transported material remains a Realization concern. No universal
accumulator, recursive-proof root, second execution semantics, or serializable
causal capability was added.

The source review changed the initial boundary in one important way. A live
Relations result cannot merely be named in Analysis prose. Relations now has
three exact, purpose-bound, owner-local result-source families, while Analysis
has one mandatory two-slot recurrence/coverage profile and one separate
CycleFold profile. This gives one checked occurrence a complete support path
without identifying the occurrence or allowing a finite chain to establish a
theorem.

## Durable absorption

| Owner | Durable destination | Retained decision |
|---|---|---|
| PIR | `pir/interfaces-and-plans.md` | One target preparation may atomically consume a finite exact map of distinct one-use predecessor supplies; portable decoding never recreates causal handoff authority. |
| Relations | `relations/recursive-composition-grounding.md` | Complete strict-or-digest-bound instance coverage, finite adjacency only, CycleFold same-step grounding, exact static question coordinates, and narrow owner-local result issuance. |
| Relations | `relations/protocol-correspondence.md` | Causal one-step recurrence has a complete static coordinate and an exact owner-local result-source family; the source-family algebra remains closed and has no generic checked-result arm. |
| Analysis | `analysis/incremental-composition.md` | Closed family selection, acyclic family-description advice, independent topology/model/depth/continuation axes, theorem use through the existing judgment calculus, derived obligations, exact Relations-result ingress, and honest report qualification. |
| Analysis | `analysis/analysis-model.md` and `analysis/cryptographic-properties.md` | The composition branch has independent exact profile catalogs and does not widen the cryptographic-property branch's family-coordinate law. |
| Realization | `realization/README.md` | Encoding, delivery, decoding, and fresh local admission move inert material only; they preserve no source occurrence or one-use capability. |

Every retained target page states that it is non-authoritative until cutover.
The temporary package remains only as research history and is deletable after
the final documentation synthesis.

## Executable evidence

The focused recursive-composition evaluator passes 35 of 35 tests. Its
positive and mutation cases cover:

- exact acyclic binding coverage and every missing, duplicate, cyclic,
  wrong-occurrence, or negative edge class;
- closed family formation, complete-body description digest derivation, and
  refusal of runtime-generated or self-embedded structure;
- all three independently checked public recurrence legs;
- one-use causal handoff versus serialized fresh re-admission;
- exact two-slot owner-local Relations-result support, forged-result and
  reconstructed-binding refusal, cross-coordinate and wrong-family refusal,
  and the separate CycleFold slot;
- theorem-coordinate rotations across topology, both depth axes, model,
  continuation quantifier, verifier, decider, assumptions, and conclusions;
  and
- conditional, carried-obligations-discharged, and genuinely hypothesis-free
  report states.

The surrounding fast semantic gate passes all five selected suites. The
dependent Interface/Plan/Relations suite passes 50 of 50 tests, and the
continuation suite passes 53 of 53 tests including a positive two-source
preparation. These are bounded executable observations, not proofs of the
durable algebra.

## Independent falsification adjudication

One bounded independent review was run after the first complete draft. Its
blocking and medium findings were reconstructed and repaired:

- the cryptographic-property page's exact family-coordinate union was scoped
  to that branch so it no longer contradicts the independent composition
  branch;
- the declared boundary matrix now executes every row and its positive
  controls instead of testing inert JSON metadata;
- qualified family checks, causal capabilities, judgments, obligations, and
  discharges are owner-issued in the evaluator, so equal caller-built records
  have no authority;
- rotation tests cover topology, predecessor arity, both depth axes, model,
  continuation quantifier, update verifier, final decider, digest rule, and
  conclusion;
- primary-source author metadata was checked against the exact PDF title pages
  and corrected;
- stale catalog counts, domain strings, and historical-note status were
  reconciled; and
- the remaining source-ingress concern was traced beyond its profile-kind
  symptom and closed with the exact Relations-to-Analysis path described
  above.

No finding required changing the published upstream interaction, Fiat--Shamir,
public-setup, commitment-opening, or Oracle-commitment profiles. The review did
not establish semantic correctness; it removed identified counterexamples to
this package's narrower claims.

## Profile consequences

The Relations law source rotates because its semantic catalog, question
coordinates, result-source families, and binding-coverage law changed. The two
incremental-composition Analysis profiles are new and must publish their exact
source-family declarations, adequacy evaluators, supported kinds, body schemas,
imports, and law bytes. Any Compiler or Realization profile that directly
interprets those laws must be rederived after them.

The six published upstream PIR profiles do not rotate. The next package owns
bottom-up publication and independent reconstruction of the changed dependent
profiles; this package supplies exact inputs but does not preclaim their IDs.

## Remaining nonclaims

Closure establishes no relation satisfaction, fold preservation, accumulator
validity, collision resistance, random-oracle realization, theorem truth,
knowledge extraction, arbitrary-party continuation, IVC/NIVC/PCD theorem,
implementation support, or production readiness. Each concrete protocol
family still needs its own correspondence witness and exact theorem-source
application after dependent profiles are published.
