The source fixtures use explicit bindings. Ports, operation order, sites, descriptors and hostile admission obligations are
retained; old proof bytes are historical countermodels, not accepted proofs.

Historical provenance of the original copies follows (its delivery hashes do
not identify these migrated files):

These four authored sources and descriptors freeze the host's tested input
interfaces; compiler output is regenerated by the injectable compiler during
integration tests. Development keys and witness inputs are
generated separately by `cargo run -p zkc-tools --example artifact_fixture -- DIR`.
