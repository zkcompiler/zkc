"""Independent executable reference for the zkc semantic kernel.

The package mirrors the public model, not the C++ implementation: one
ProtocolVocabulary, profile-qualified claims, reduction and terminal closure,
the canonical encoding, and the OIR operational image.  It also mirrors the soundness
signature — the declaration documents and their content addresses, RULE_WF,
and binding well-formedness — and the structural and typing half of a
derivation.

It stops where a bound begins.  A second implementation of the same arithmetic
shares a common-mode misreading with the first, so a derived figure is
cross-checked by re-derivation from its cited source instead
(docs/spec/boundaries.md).
"""
