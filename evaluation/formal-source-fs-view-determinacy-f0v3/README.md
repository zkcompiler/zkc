# F0-V3 Fiat--Shamir family view determinacy

This package asks one exact question:

> Do the exact current canonical-framed and duplex-sponge profile sources
> determine every displayed field of their eight family-view bodies as an exact
> finite body, while the normalized candidate bodies compile and agree on every
> admitted executable carrier that exists?

The frozen aggregate answer is
`CannotAnswer/F0V3-C-FS-VIEW-DETERMINACY`. The separate bounded-grammar result
is `Affirmative/F0V3-A-FS-VIEW-GRAMMAR-BOUNDED`.

Run from the repository root:

```sh
python3 -B evaluation/formal-source-fs-view-determinacy-f0v3/run.py --check
```

## What is frozen

[`field-audit.json`](field-audit.json) selects all 97 displayed fields by exact
owner-page line. It classifies 37 as `exact-body`, 40 as
`prose-placeholder`, and 20 as `undefined-symbol`; independently, 44 candidate
leaves are atomic under the F0-V2A rules and 53 require structural grammar.
Every non-exact field closes to one of the exact owner obligations below.

[`schema-source.json`](schema-source.json) is a finite, acyclic candidate for
the eight normalized bodies. It uses only the F0-V2A constructors `Atom`,
`Record`, `Variant`, and bounded `Sequence`. Its atoms are natural numbers,
unit, meta-Booleans, typed canonical bodies, and exact profile-law references.
The two result candidates omit the process-local `result_ref` and prose
`result_schema`: their normalized records begin with the four content
identities, followed by structural maps and a closed conclusion. That is a
candidate repair, not an interpretation of the current display.

The recursive compiler in [`model.py`](model.py) and independently structured
iterative/topological compiler in [`independent.py`](independent.py) produce
the same eight expanded schemas. The frozen schema digest is
`0afe8797203334dea54fec67e1c8706ea4af7fae6c4f1a189f66cb6e7b0b1f69`.
The check freezes each schema's node and atomic-leaf count.

The typed path admits the K2 Schnorr and Oracle fixtures, obtains their checked
FS construction carriers, and derives all four canonical candidate values.
The cold path derives the same values from inert normalized carrier records
without importing the K2 model. For duplex, the typed path admits the public
construction fixture and the cold path independently reconstructs its content
identities and three construction-owned candidate values directly from JSON.
Every paired value agrees exactly and is accepted by both validators; its body
digest and inhabited leaf count are frozen. The duplex witness has no admitted
checked-result issuer, so this package does not fabricate or claim a duplex
result value.

Schema-ordinal, exact-law, canonical-body-compiler, and owner-substitution
mutations are rejected by both paths. A witness-local value is never used to
upgrade a missing owner definition.

## Exact owner obligations

The current pages and manifests do not publish:

1. a `pir.static-view-schema` subject and body compiler for either family;
2. canonical initialization and frame-schedule coordinate bodies for the
   canonical-framed declaration view;
3. canonical scope, ordered influence-set, addition, and prefix-law-reference
   bodies;
4. canonical namespace, accept/decode ABI, draw-bound, transition-law, and
   decoding-coordinate bodies;
5. a canonical checked canonical-framed result description, identity maps, and
   conclusion, including a body-safe replacement for the owner-local result
   pointer;
6. duplex family-tag, state-carrier, instance-carrier, bit-convention, and
   binding-projection bodies;
7. duplex construction-material, salt-coordinate, and target-only material-map
   bodies;
8. duplex message-codec, semantic-argument-shape, and encoded-coverage bodies;
9. duplex schedule, binding-sequence, coverage-law, prohibited-addition, and
   resource-projection bodies;
10. duplex decoder-totality, accept/retry/failure tag, execution-domain, and
    squeeze-event bodies; and
11. a canonical checked duplex result description, maps, projections,
    correspondences, and conclusion, including a body-safe replacement for the
    owner-local result pointer.

The exact file, section, and line coordinates are frozen in
[`field-audit.json`](field-audit.json). Missing evidence remains
`CannotAnswer`; neither K2, the duplex fixture, nor a similarly named
Interaction body is owner authority for these facts.

## What a pass establishes

A pass establishes only that the pinned source inventory still has the frozen
37/40/20 determinacy split; the eight candidate schemas stay inside the finite
description universe; recursive and iterative compilation agree; the selected
typed and cold witness projections agree; all candidate values validate; the
frozen digests and leaf counts remain unchanged; and the selected mutations
are killed.

It does **not** publish or adopt the candidate grammar, repair either PIR owner
page, prove that the current eight displays are determinate, establish compiler
or backend support, authenticate a source theorem, prove theorem
applicability, or establish Fiat--Shamir soundness, knowledge soundness, zero
knowledge, ROM/QROM security, or any production-security claim.
