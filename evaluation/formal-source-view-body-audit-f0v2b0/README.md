# F0-V2B0 PIR owner-view body audit

This source audit tests whether the six current Interaction owner-view displays
can be compiled verbatim into the exact finite schema algebra selected by
F0-V2A. Its aggregate result is
`CannotAnswer/F0V2B0-C-OWNER-BODY-DETERMINACY`.

Run from the repository root:

```sh
python3 -B evaluation/formal-source-view-body-audit-f0v2b0/run.py --check
```

The audit freezes five exact source digests and eighteen classifications. It
cross-checks:

- the authenticated Interaction static-view fragment;
- Appendix A's exact Core, Protocol, declaration, occurrence, and `PCNode`
  basis;
- all six displayed view bodies;
- nested Analysis and Relations field selections;
- the live Interaction profile manifest; and
- the constructor surface of the exact F1-R1B admitted fixture.

It confirms that exact stored owner bodies, six displays, nested consumer
pressure, and the authenticated module-effect boundary exist. It returns
`CannotAnswer` for undefined `PIRReference`, `ProverViewCoordinate`, and
`guard_ref` vocabulary; absent canonical view body functions; ten prose-valued
derived fields; omitted retained PCGraph evidence; absent Fresh resolver and
run-record description bodies; incomplete F1-R1B constructor pressure; and the
still-unpublished static-view schema catalog. It refuses treating the displays,
a consumer shadow schema, or reflected module payload as the missing owner
grammar.

The selected replacement is documented in
[`f0v2b-owner-view-body-audit-and-redesign.md`](../../docs-next/notes/semantic-revalidation-and-redesign/formal-assurance-research/f0v2b-owner-view-body-audit-and-redesign.md).
This package does not repair target source, define the normalized canonical
bodies, derive a view, publish a profile, establish source correspondence, or
verify an implementation.
